# 개발 진행 로그 36

## 목표

- 신고 준비 ZIP 패키지 기능 푸시 후 Railway 공개 URL이 최신 Next 서버를 서빙하는지 다시 확인한다.

## 확인 결과

- 최신 푸시 커밋: `61f1496 Add filing package zip export`
- 공개 URL: `https://honzang-production.up.railway.app`

## 공개 URL 응답

- `/api/version`: 404
- `/api/health`: 404
- `/`: 200
- 루트 HTML에는 `혼자장부` 문구가 포함되어 있으나, API 라우트가 404이므로 현재 도메인은 최신 Next standalone 서버가 아니라 이전 정적 배포 또는 다른 Railway 서비스에 연결된 상태로 판단한다.

## 다음 조치

- Railway 대시보드에서 서비스 소스가 `studioMong/honzang` 저장소의 `main` 브랜치인지 확인한다.
- 최신 배포 커밋이 `61f1496`인지 확인한다.
- 배포 빌더가 Dockerfile을 사용하고 있는지 확인한다.
- 공개 도메인 `honzang-production.up.railway.app`이 현재 Next 앱 서비스에 연결되어 있는지 확인한다.
- `/api/version`이 200을 반환하기 전까지는 공개 배포 완료로 보지 않는다.
