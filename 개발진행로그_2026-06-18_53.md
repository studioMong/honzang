# 개발 진행 로그 2026-06-18 53

## 작업 범위
- 원본 CSV 보관 데이터 검증을 강화했다.
- 가져오기 API에서 `originalFileText`의 실제 UTF-8 바이트 크기와 `originalFileSize` 불일치를 거절하도록 했다.
- 백업 복원 dry-run에서도 원본 CSV 크기 불일치를 `INVALID_BACKUP_ORIGINAL_FILES` 코드로 거절하도록 했다.
- UTF-8 BOM 차이 가능성을 고려해 3바이트까지의 차이는 허용했다.
- production smoke와 backup restore 검증 스크립트에 실패 케이스를 추가했다.

## 변경 파일
- `src/lib/server/source-file-validation.ts`
- `src/app/api/imports/route.ts`
- `src/app/api/backups/restore/route.ts`
- `scripts/smoke-production.mjs`
- `scripts/verify-backup-restore.mjs`

## 검증
- `npm run typecheck` 통과
- `npm run lint` 통과
- `git diff --check` 통과
- `npm run verify:mvp` 통과

## 메모
- 원본 CSV는 신고/감사 근거로 쓰이므로 서버와 복원 경로 모두 실제 텍스트 크기를 기준으로 검증한다.
- 백업 검증 fixture의 한글 CSV 크기도 실제 UTF-8 바이트 수로 계산하도록 수정했다.
