# 개발진행로그 72

## 작업 시각

- 2026-06-17 23:15 KST

## 확인 대상

- 커밋: `bd6aff7` (`Add filing submission guide`)
- 공개 URL: `https://honzang-production.up.railway.app`
- 검증 명령: `RAILWAY_EXPECTED_COMMIT=bd6aff7 npm run verify:railway`

## 결과

- Railway 공개 URL 검증 실패.
- `/`는 HTTP 200이지만 legacy 정적 프로젝트 요약 페이지로 응답.
- `/api/version`은 HTTP 404.
- `/api/health`는 HTTP 404.
- `/manifest.webmanifest`는 HTTP 404.

## 판단

- 같은 커밋에서 로컬 `npm run verify:mvp`는 통과했다.
- 로컬 production smoke는 `홈택스 제출 전 입력 가이드` 문구까지 확인했다.
- 공개 URL이 계속 legacy 정적 페이지를 반환하므로, 최신 Next.js 서비스가 해당 도메인으로 노출되지 않고 있다.

## 필요한 외부 확인

- Railway Dashboard에서 공개 도메인이 최신 Next.js 서비스에 붙어 있는지 확인.
- GitHub `studioMong/honzang` `main` 브랜치 자동 배포가 해당 서비스에 연결되어 있는지 확인.
- Root Directory, Dockerfile 빌드, `DATABASE_URL`, `npm run db:deploy`, `npm run start` 설정을 확인.
