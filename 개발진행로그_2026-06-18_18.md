# 개발 진행 로그 - 2026-06-18 18

## 목표
- 설정 화면 CSV 템플릿 목록 커밋 `6b9d862eb96adfa95080d7286b81fefb0925dd8d` 기준으로 Railway 공개 주소 배포 상태를 재감사한다.

## 실행
- `RAILWAY_AUDIT_SOFT=1 RAILWAY_EXPECTED_COMMIT=6b9d862eb96adfa95080d7286b81fefb0925dd8d npm run audit:railway`

## 결과
- 감사 대상: `https://honzang-production.up.railway.app`
- `/`: HTTP 200, `text/html; charset=utf-8`, 24489 bytes
- `/api/version`: HTTP 404
- `/api/health`: HTTP 404
- `/manifest.webmanifest`: HTTP 404
- 요약: 4 fail, 0 warn, 0 pass

## 판단
- 공개 도메인은 아직 최신 Next.js 앱을 서빙하지 않는다.
- 앱 서버의 API 라우트와 PWA manifest가 노출되지 않아 Railway public domain이 현재 서비스가 아닌 legacy/static 서비스에 연결된 상태로 판단한다.
- 코드와 로컬 standalone 검증은 통과하므로 남은 배포 문제는 Railway Dashboard의 서비스/도메인 연결 확인이 필요하다.

## 다음 조치
- Railway Dashboard에서 `honzang-production.up.railway.app` 도메인이 최신 Next.js 서비스에 연결되어 있는지 확인한다.
- Deploy source commit이 GitHub `main`의 최신 커밋과 일치하는지 확인한다.
- 도메인 연결 수정 후 `npm run audit:railway`와 `npm run smoke:prod`를 다시 실행한다.
