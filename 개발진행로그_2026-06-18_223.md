# 개발 진행 로그 223

## 작업 시각
- 2026-06-18

## 감사 대상
- 공개 URL: `https://honzang-production.up.railway.app`
- 기대 커밋: `b0e5ffdc7b1699d31d734daef5515dbcef4362ac`
- 실행 명령:
  - `RAILWAY_AUDIT_SOFT=1 RAILWAY_EXPECTED_COMMIT=b0e5ffdc7b1699d31d734daef5515dbcef4362ac npm run audit:railway`

## 감사 결과
- Summary: `5 fail, 0 warn, 0 pass`
- `/`
  - HTTP 200
  - `text/html; charset=utf-8`
  - 24489 bytes
  - `server=railway-hikari`
  - `x-railway-request-id=aiVjzPhxSYGXSA-FVOLIQQ`
  - preview에 `<title>혼자장부 프로젝트 정리</title>` 포함
- `/api/version`
  - HTTP 404
  - `x-railway-request-id=8driiMS0TgWHplSCAQeqjw`
- `/api/health`
  - HTTP 404
  - `x-railway-request-id=9loLyu82T3iHtv_iJH0Vcg`
- `/manifest.webmanifest`
  - HTTP 404
  - `x-railway-request-id=K7nI8iWIR-WrPUouoB_USg`

## 실패 항목
- Next.js API 라우트 미노출
- Legacy 정적 페이지 응답
- Healthcheck 미노출
- PWA manifest 미노출
- 보안 헤더 누락

## 판단
- GitHub `main`에는 분개 금액 정밀도 검증 커밋 `b0e5ffd`가 푸시되어 있다.
- Railway 공개 도메인은 아직 최신 Next.js 앱이 아니라 이전 정적/레거시 서비스로 연결되어 있다.
- 코드 변경으로 해결할 문제가 아니라 Railway Dashboard에서 Public Networking 대상 서비스를 최신 Next.js 서비스로 전환해야 한다.

## 다음 조치
- Railway Dashboard에서 `honzang-production.up.railway.app` 공개 도메인의 연결 대상 서비스를 확인한다.
- 최신 Next.js 서비스가 Dockerfile 빌드, `npm run build`, `npm run db:deploy`, `npm run start`, `/api/health` healthcheck를 사용하도록 확인한다.
- 전환 후 `npm run audit:railway` 또는 `npm run verify:railway`를 다시 실행한다.
