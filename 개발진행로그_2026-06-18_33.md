# 개발 진행 로그 - 2026-06-18 33

## 목표
- 사업자등록번호, 업종 등 법인 기본정보가 신고 준비 점검과 마감 차단 조건에 직접 반영되게 한다.
- 홈택스 제출 전 입력 가이드에서 가장 먼저 확인할 기본정보 단계를 보여준다.

## 변경 내용
- `src/components/app-workspace.tsx`
  - `buildFilingReadinessRows`에 회사 설정 상태를 전달한다.
  - 최종 신고 점검 첫 항목으로 `법인 기본정보`를 추가했다.
  - 법인명, 사업자등록번호, 업종 등 필수 기본정보가 누락되면 최종 신고 점검이 `차단`으로 표시된다.
  - 겸영, 원천세 대상, 과금 단가 같은 확인 항목은 `확인 필요`로 표시된다.
  - 마감 잠금 차단 목록에도 법인 기본정보 누락이 포함된다.
  - 홈택스 제출 전 입력 가이드 첫 단계에 `기본정보`를 추가해 사업자등록증/법인 기본정보 대조를 먼저 보게 했다.
- `scripts/smoke-production.mjs`
  - 리포트 화면 스모크에 `법인 기본정보`, `기본정보` 문구 확인을 추가했다.

## 검증
- `npm run typecheck` 통과.
- `npm run lint` 통과.
- `git diff --check` 통과.
- 인앱 브라우저에서 `http://localhost:3000/?view=reports` 렌더링 확인.
  - `혼자장부 신고 준비 리포트` 확인.
  - `법인 기본정보` 확인.
  - 제출 가이드의 `기본정보`, `사업자등록증` 확인.
  - `마감/보관` 확인.
- 외부 실행 `npm run verify:mvp` 통과.

## 판단
- 신고 준비 리포트가 거래/증빙/분개뿐 아니라 법인 필수정보 누락도 마감 전 차단 조건으로 다룬다.
- 사용자가 신고 전에 가장 먼저 대조해야 하는 기본정보를 리포트와 제출 가이드에서 확인할 수 있다.
