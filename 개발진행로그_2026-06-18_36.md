# 개발 진행 로그 - 2026-06-18 36

## 목표
- 수기 거래 검증 커밋 `bffc2bc7e97f10d3890f3f99cdf6e65f95f455f7` 기준으로 Railway 공개 주소 배포 상태를 재감사한다.

## 실행
- `RAILWAY_AUDIT_SOFT=1 RAILWAY_EXPECTED_COMMIT=bffc2bc7e97f10d3890f3f99cdf6e65f95f455f7 npm run audit:railway`

## 결과
- 감사 대상: `https://honzang-production.up.railway.app`
- `/`: HTTP 200, `text/html; charset=utf-8`, 24489 bytes
- `/api/version`: HTTP 404
- `/api/health`: HTTP 404
- `/manifest.webmanifest`: HTTP 404
- 요약: 4 fail, 0 warn, 0 pass

## 판단
- 공개 도메인은 최신 Next.js 앱이 아니라 기존 legacy/static 응답을 계속 반환한다.
- API 라우트와 PWA manifest가 공개 URL에 연결되지 않았다.
- 로컬 `npm run verify:mvp`는 통과했으므로 Railway Dashboard의 public domain 연결 대상 수정이 남은 배포 완료 조건이다.

## 다음 조치
- Railway Dashboard에서 `honzang-production.up.railway.app` public domain을 최신 Next.js 서비스로 연결한다.
- 최신 서비스 Healthcheck Path를 `/api/health`로 맞춘다.
- 연결 수정 후 감사 스크립트를 다시 실행한다.
