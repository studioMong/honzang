# 개발진행로그 62

## 작업 시각

- 2026-06-17 22:45 KST

## 확인 대상

- 커밋: `d799036` (`Add report data source coverage`)
- 공개 URL: `https://honzang-production.up.railway.app`
- 검증 명령: `RAILWAY_EXPECTED_COMMIT=d799036 npm run verify:railway`

## 결과

- Railway 공개 URL 검증 실패.
- `/`는 HTTP 200이지만 legacy 정적 프로젝트 요약 페이지로 응답.
- `/api/version`은 HTTP 404.
- `/api/health`는 HTTP 404.
- `/manifest.webmanifest`는 HTTP 404.

## 판단

- 저장소의 Next.js standalone 빌드와 API 라우트는 `npm run verify:mvp`에서 통과했다.
- Railway 공개 도메인이 현재 저장소의 최신 `main` 브랜치 Next.js 서비스가 아닌 기존 정적 서비스나 다른 빌드 결과에 연결된 상태로 보인다.

## Railway Dashboard에서 확인할 항목

- 서비스가 `git@github.com:studioMong/honzang.git` 저장소의 `main` 브랜치에 연결되어 있는지.
- Root Directory가 비어 있거나 저장소 루트로 지정되어 있는지.
- Build/Start Command가 Dockerfile 또는 `npm run start` 기반 standalone 배포를 사용하고 있는지.
- `DATABASE_URL`이 Railway Postgres 값으로 설정되어 있는지.
- `honzang-production.up.railway.app` 도메인이 최신 Next.js 서비스에 붙어 있는지.
