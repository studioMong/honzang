# 개발 진행 로그 - 2026-06-18 34

## 목표
- 신고 기본정보 readiness 커밋 `040932f387c9917d5e49ea031b8da1a12ca65a15` 기준으로 Railway 공개 주소 배포 상태를 재감사한다.

## 실행
- `RAILWAY_AUDIT_SOFT=1 RAILWAY_EXPECTED_COMMIT=040932f387c9917d5e49ea031b8da1a12ca65a15 npm run audit:railway`

## 결과
- 감사 대상: `https://honzang-production.up.railway.app`
- `/`: HTTP 200, `text/html; charset=utf-8`, 24489 bytes
- `/api/version`: HTTP 404
- `/api/health`: HTTP 404
- `/manifest.webmanifest`: HTTP 404
- 요약: 4 fail, 0 warn, 0 pass

## 판단
- 공개 도메인은 최신 Next.js 앱이 아니라 기존 legacy/static 응답을 계속 반환한다.
- `/api/version`, `/api/health`, `/manifest.webmanifest`가 모두 404이므로 앱 라우트가 공개 URL에 연결되지 않았다.
- 로컬 `npm run verify:mvp`는 통과했으므로 코드 산출물보다 Railway Dashboard의 public domain 연결 대상 문제가 남아 있다.

## 다음 조치
- Railway Dashboard에서 `honzang-production.up.railway.app` public domain을 최신 Next.js 서비스로 연결한다.
- 최신 서비스 Healthcheck Path를 `/api/health`로 맞춘다.
- 연결 수정 후 감사 스크립트를 다시 실행한다.
