# 개발 진행 로그 108

## 작업 단위
- 거래 계정 변경 차단 커밋(`4008affa3897a3f7451c704ac427f66eae14a412`) 이후 공개 Railway 배포 상태를 감사했다.

## 실행 명령
- `RAILWAY_AUDIT_SOFT=1 RAILWAY_EXPECTED_COMMIT=4008affa3897a3f7451c704ac427f66eae14a412 npm run audit:railway`

## 감사 결과
- 공개 주소: `https://honzang-production.up.railway.app`
- 기대 커밋: `4008affa3897a3f7451c704ac427f66eae14a412`
- `/`: HTTP 200, `text/html; charset=utf-8`, 24489 bytes
- `/api/version`: HTTP 404
- `/api/health`: HTTP 404
- `/manifest.webmanifest`: HTTP 404
- 요약: 5 fail, 0 warn, 0 pass

## 실패 항목
- `Next.js API 라우트 미노출`
- `Legacy 정적 페이지 응답`
- `Healthcheck 미노출`
- `PWA manifest 미노출`
- `보안 헤더 누락`

## 판단
- GitHub `main`에는 최신 커밋이 푸시됐지만 공개 Railway 도메인은 아직 최신 Next.js standalone 앱을 바라보지 않는다.
- 루트 페이지의 응답 크기와 API 404 상태가 이전 감사와 동일하다.
- 보안 헤더 누락도 실제 코드 문제가 아니라 공개 URL이 최신 앱에 연결되지 않은 결과로 판단한다.

## 다음 조치
- Railway Dashboard에서 `honzang-production.up.railway.app` public domain을 최신 Next.js 서비스로 연결한다.
- 앱 서비스 Variables에 Railway Postgres `DATABASE_URL` 참조 변수가 연결되어 있는지 확인한다.
- 전환 후 `RAILWAY_EXPECTED_COMMIT=$(git rev-parse HEAD) npm run verify:railway`를 실행한다.
