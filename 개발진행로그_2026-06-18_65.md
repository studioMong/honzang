# 개발 진행 로그 2026-06-18 65

## 작업 범위
- 백업 복원 dry-run에서 거래처 기본 계정과 자동분류 규칙 계정 참조가 유효한지 검증하도록 보강했다.

## 구현 내용
- `src/app/api/backups/restore/route.ts`
  - `validateBackupAccountReferences`를 추가했다.
  - 거래처 `defaultAccount.code`가 기본 계정 또는 백업 계정 목록에 없으면 `INVALID_BACKUP_ACCOUNT_REFERENCES` 이슈로 반환한다.
  - 자동분류 규칙 `accountCode`가 기본 계정 또는 백업 계정 목록에 없으면 `INVALID_BACKUP_ACCOUNT_REFERENCES` 이슈로 반환한다.
  - 실제 복원 단계에서 거래처 기본 계정이 `null`이 되거나 자동분류 규칙이 스킵되는 상황을 dry-run에서 미리 차단한다.

## 테스트 보강
- `scripts/verify-backup-restore.mjs`
  - 거래처와 자동분류 규칙이 존재하지 않는 계정 코드를 참조하는 백업 JSON을 HTTP 400과 `INVALID_BACKUP_ACCOUNT_REFERENCES`로 거절하는지 확인한다.

## 검증
- `npm run typecheck`
- `npm run lint`
- `git diff --check`
- `npm run verify:mvp`

## 결과
- 전체 MVP 검증이 통과했다.
- 공개 Railway 도메인의 legacy/static 연결 문제는 별도 감사 단계에서 계속 확인해야 한다.
