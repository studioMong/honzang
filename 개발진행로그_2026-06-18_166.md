# 개발 진행 로그 166

## 작업 단위
- 설정 화면의 과금 정책 섹션이 프로덕션 스모크 검증에서 빠지지 않도록 회귀 검사를 보강했다.
- 코드 커밋(`a5b9dd7619744bae710b08de4f89e1d206fc4ba5`) 이후 공개 Railway 배포 상태를 다시 감사했다.

## 구현 내용
- `scripts/smoke-production.mjs`의 설정 화면 확인 문구에 `과금 정책`, `회당 단가`, `SaaS 월 구독`을 추가했다.
- 사용자가 요청한 내부 회당 정산과 SaaS 구독 과금 설정이 화면 회귀 검증에 포함되도록 했다.

## 검증
- `npm run typecheck`: 통과
- `npm run lint`: 통과
- `git diff --check`: 통과
- `npm run verify:mvp`: 통과
- `DATABASE_URL`: 현재 셸에 없어 `npm run verify:db-workflow`는 실행하지 못했다.

## Railway 감사
- 실행 명령: `RAILWAY_AUDIT_SOFT=1 RAILWAY_EXPECTED_COMMIT=a5b9dd7619744bae710b08de4f89e1d206fc4ba5 npm run audit:railway`
- 공개 주소: `https://honzang-production.up.railway.app`
- 기대 커밋: `a5b9dd7619744bae710b08de4f89e1d206fc4ba5`
- `/`: HTTP 200, `text/html; charset=utf-8`, 24489 bytes, `server=railway-hikari`, `x-railway-request-id=9NpHlASRRem-urIeJH0Vcg`
- 루트 미리보기: `<title>혼자장부 프로젝트 정리</title>`
- `/api/version`: HTTP 404, `server=railway-hikari`, `x-railway-request-id=8Ctlc_8DTqioRalc0ubPiw`
- `/api/health`: HTTP 404, `server=railway-hikari`, `x-railway-request-id=C5xOz8OjSFK10uEuAXC71g`
- `/manifest.webmanifest`: HTTP 404, `server=railway-hikari`, `x-railway-request-id=JD5X4YlGQ3itE-aiJH0Vcg`
- 요약: 5 fail, 0 warn, 0 pass

## 판단
- 최신 커밋은 GitHub `main`에 푸시됐지만 공개 Railway 도메인은 여전히 이전 정적 페이지 서비스에 연결되어 있다.
- 로컬 standalone 빌드와 MVP 검증은 통과하므로, 남은 공개 배포 이슈는 Railway Dashboard의 public domain 연결 대상 문제다.

## 다음 조치
- Railway Dashboard에서 `honzang-production.up.railway.app` public domain이 붙은 서비스를 최신 Next.js standalone 앱 서비스로 전환한다.
- 전환 후 `RAILWAY_EXPECTED_COMMIT=$(git rev-parse HEAD) npm run verify:railway`를 실행한다.
