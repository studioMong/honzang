# 개발진행로그 2026-06-18 245

## 목표

- `Allow settings rule and vendor editing` 커밋 푸시 후 Railway 공개 도메인이 최신 Next.js 앱을 서빙하는지 확인한다.

## 대상 커밋

- `116a5e983ad61f0d91339b40f64b430f1fb401de`

## 실행

```bash
RAILWAY_AUDIT_SOFT=1 RAILWAY_EXPECTED_COMMIT=116a5e983ad61f0d91339b40f64b430f1fb401de npm run audit:railway
```

## 결과

- `https://honzang-production.up.railway.app/`
  - HTTP 200
  - `server=railway-hikari`
  - 응답 제목: `혼자장부 프로젝트 정리`
  - 이전 static/legacy 페이지로 판단
- `/api/version`
  - HTTP 404
- `/api/health`
  - HTTP 404
- `/manifest.webmanifest`
  - HTTP 404

## 판정

- 감사 요약: `5 fail, 0 warn, 0 pass`
- soft mode로 실행했기 때문에 명령 자체는 성공 종료했다.
- GitHub `main`에는 최신 코드 커밋이 푸시되었지만, Railway 공개 도메인은 아직 최신 Next.js 앱 서비스에 연결되지 않았다.

## 필요한 외부 조치

- Railway Dashboard에서 `honzang-production.up.railway.app` Public Networking 대상 서비스를 최신 Next.js 앱으로 전환한다.
- 이전 static/legacy 서비스가 public domain을 계속 점유하고 있으면 제거하거나 도메인 연결을 해제한다.
- 전환 후 `/api/version`, `/api/health`, `/manifest.webmanifest`가 200으로 응답해야 한다.
- 상세 절차는 `docs/railway-cutover.md` 기준으로 확인한다.
