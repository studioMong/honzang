# 개발 진행 로그 - 2026-06-18 17

## 목표
- 저장된 CSV 매핑 템플릿을 설정 화면에서 확인하고 내려받을 수 있게 한다.
- 반복 업로드에 어떤 헤더/컬럼 매핑이 적용되는지 운영 중에 추적 가능하게 만든다.

## 변경 내용
- `src/components/app-workspace.tsx`
  - 설정 화면에 `CSV 매핑 템플릿` 섹션을 추가했다.
  - 자료 유형, 템플릿명, 헤더 요약, 필수 매핑, 금액 매핑, 선택 매핑, 수정 시각을 표로 보여준다.
  - `템플릿 목록` CSV 다운로드 버튼을 추가했다.
  - 저장된 템플릿이 없을 때 빈 상태 문구를 제공한다.
- `README.md`
  - MVP 범위에 CSV 매핑 템플릿 저장, 자동 적용 상태 확인, 템플릿 목록 다운로드를 추가했다.
- `scripts/smoke-production.mjs`
  - 설정 화면 smoke 검증에 `CSV 매핑 템플릿` 문구 확인을 추가했다.

## 검증
- `npm run typecheck` 통과.
- `npm run lint` 통과.
- `git diff --check` 통과.
- `npm run verify:mvp` 통과.
- 로컬 production 서버 `http://127.0.0.1:3117/?view=settings` 브라우저 확인.
  - 데스크톱 1280px: `CSV 매핑 템플릿`, `템플릿 목록`, 빈 상태 문구 표시, 가로 넘침 없음.
  - 모바일 390px: 동일 상태 유지, 가로 넘침 없음.

## 판단
- 업로드 화면에서 템플릿 적용 상태를 보는 흐름과 설정 화면에서 템플릿 목록을 점검하는 흐름이 연결됐다.
- 실제 CSV 구조가 늘어나도 어떤 자료 유형에 어떤 매핑이 저장됐는지 운영자가 확인하고 내보낼 수 있다.
