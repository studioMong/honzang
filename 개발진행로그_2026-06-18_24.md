# 개발 진행 로그 - 2026-06-18 24

## 목표
- 접근제어 검증 보강 커밋 `ed56cde6f670c37339903a8d25d4615f4f811201` 기준으로 Railway 공개 주소 배포 상태를 재감사한다.

## 실행
- `RAILWAY_AUDIT_SOFT=1 RAILWAY_EXPECTED_COMMIT=ed56cde6f670c37339903a8d25d4615f4f811201 npm run audit:railway`

## 결과
- 감사 대상: `https://honzang-production.up.railway.app`
- `/`: HTTP 200, `text/html; charset=utf-8`, 24489 bytes
- `/api/version`: HTTP 404
- `/api/health`: HTTP 404
- `/manifest.webmanifest`: HTTP 404
- 요약: 4 fail, 0 warn, 0 pass

## 판단
- 공개 도메인은 아직 최신 Next.js 앱을 서빙하지 않는다.
- 앱 서버 API 라우트와 PWA manifest가 노출되지 않아, 현재 public domain이 최신 Next.js 서비스가 아닌 legacy/static 서비스에 연결된 상태로 판단한다.
- 로컬 `npm run verify:mvp`는 통과하므로 코드/빌드 산출물보다 Railway Dashboard의 도메인 연결 상태가 남은 이슈다.

## 다음 조치
- Railway Dashboard에서 `honzang-production.up.railway.app` public domain 연결 대상 서비스를 최신 Next.js 서비스로 변경한다.
- 연결 수정 뒤 `/api/version`, `/api/health`, `/manifest.webmanifest`가 공개 URL에서 응답하는지 확인한다.
