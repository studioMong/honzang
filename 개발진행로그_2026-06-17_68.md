# 개발진행로그 68

## 작업 시각

- 2026-06-17 23:22 KST

## 확인 대상

- 커밋: `f141fd5` (`Expand production smoke UI coverage`)
- 공개 URL: `https://honzang-production.up.railway.app`
- 검증 명령: `RAILWAY_EXPECTED_COMMIT=f141fd5 npm run verify:railway`

## 결과

- Railway 공개 URL 검증 실패.
- `/`는 HTTP 200이지만 legacy 정적 프로젝트 요약 페이지로 응답.
- `/api/version`은 HTTP 404.
- `/api/health`는 HTTP 404.
- `/manifest.webmanifest`는 HTTP 404.

## 판단

- 로컬 `npm run verify:mvp`는 강화된 production smoke UI 검사까지 통과했다.
- 현재 저장소의 Next.js standalone 빌드에는 API 라우트와 PWA manifest가 존재한다.
- 공개 URL이 계속 legacy 정적 페이지를 반환하므로 Railway 도메인/서비스 연결 문제가 계속 남아 있다.

## 필요한 외부 확인

- Railway Dashboard에서 공개 도메인이 현재 GitHub `studioMong/honzang` `main` 브랜치 서비스에 연결되어 있는지 확인.
- 루트 `Dockerfile` 빌드, Root Directory, `npm run start`, `npm run db:deploy`, `DATABASE_URL` 설정을 확인.
- 수정 후 `RAILWAY_EXPECTED_COMMIT=$(git rev-parse HEAD) npm run verify:railway`가 통과해야 공개 배포 완료로 본다.
