# 개발 진행 로그 177

## 작업 단위
- 대시보드 자료 수집 상태를 실제 업로드 이력과 더 직접적으로 연결했다.
- 코드 커밋(`63507b779b850b7f6efe0a6c4ad0099377117cb0`) 이후 공개 Railway 배포 상태를 다시 감사했다.

## 구현 내용
- `src/components/app-workspace.tsx`의 `Dashboard`에 `importBatches`를 전달했다.
- `buildDataSourceRows`가 선택적으로 `importBatches`를 받아 자료 유형별 업로드 배치 수와 원본 CSV 보관 수를 계산하도록 했다.
- 대시보드 `자료 수집 현황` 행의 세부 설명에 다음 항목을 추가했다.
  - 업로드 이력 수
  - 원본 CSV 보관 수
  - 기존 기간과 다음 확인 작업
- `scripts/smoke-production.mjs` 루트 화면 검증에 `업로드 이력`, `원본 CSV` 문구를 추가했다.

## 검증
- `npm run typecheck`: 통과
- `npm run lint`: 통과
- `git diff --check`: 통과
- `npm run verify:mvp`: 통과
- `DATABASE_URL`: 현재 셸에 없어 `npm run verify:db-workflow`는 실행하지 못했다.

## 브라우저 확인
- 로컬 production 서버: `http://127.0.0.1:3122/`
- 확인 결과:
  - 문서 제목: `혼자장부`
  - `자료 수집 현황`: 표시됨
  - `필수 자료`: 표시됨
  - `업로드 이력`: 표시됨
  - `원본 CSV`: 표시됨
  - `통장`: 표시됨
  - `홈택스 매출`: 표시됨
  - 브라우저 콘솔 에러: 0건

## Railway 감사
- 실행 명령: `RAILWAY_AUDIT_SOFT=1 RAILWAY_EXPECTED_COMMIT=63507b779b850b7f6efe0a6c4ad0099377117cb0 npm run audit:railway`
- 공개 주소: `https://honzang-production.up.railway.app`
- 기대 커밋: `63507b779b850b7f6efe0a6c4ad0099377117cb0`
- `/`: HTTP 200, `text/html; charset=utf-8`, 24489 bytes, `server=railway-hikari`, `x-railway-request-id=tBM-x1b9TiCjUM1-V7rehQ`
- 루트 미리보기: `<title>혼자장부 프로젝트 정리</title>`
- `/api/version`: HTTP 404, `server=railway-hikari`, `x-railway-request-id=ZEi2Bf_HQrCHrYa6VOLIQQ`
- `/api/health`: HTTP 404, `server=railway-hikari`, `x-railway-request-id=0UbNckbsSiioIk7gacI7Nw`
- `/manifest.webmanifest`: HTTP 404, `server=railway-hikari`, `x-railway-request-id=nD7jpCw7QUGBbINqJH0Vcg`
- 요약: 5 fail, 0 warn, 0 pass

## 판단
- 최신 커밋은 GitHub `main`에 푸시됐지만 공개 Railway 도메인은 여전히 이전 정적 페이지 서비스에 연결되어 있다.
- 대시보드에서 자료별 거래 반영 상태뿐 아니라 업로드 이력과 원본 CSV 보관 상태도 함께 볼 수 있게 됐다.
- 로컬 production 빌드, MVP 검증, 브라우저 렌더링 검증은 정상이다.

## 다음 조치
- Railway Dashboard에서 `honzang-production.up.railway.app` public domain을 최신 Next.js standalone 앱 서비스로 전환한다.
- 전환 후 `RAILWAY_EXPECTED_COMMIT=$(git rev-parse HEAD) npm run verify:railway`를 실행한다.
- 다음 개발 단위에서는 DB 연결 환경에서 실제 업로드 이력과 대시보드 표시가 같은 기준으로 동작하는지 검증할 수 있는 자동 시나리오를 보강한다.
