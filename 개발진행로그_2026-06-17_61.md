# 개발진행로그 61

## 작업 시각

- 2026-06-17 22:40 KST

## 목표

- 신고 준비 화면에서 통장, 카드, 홈택스, PG 자료가 실제 거래에 반영됐는지 사용자가 먼저 확인할 수 있게 한다.
- 화면에 보이는 자료 수집 현황을 세무 리포트 저장본, JSON, CSV ZIP, XLSX 패키지에도 같이 포함한다.

## 구현 내용

- 리포트 화면에 `자료 수집 현황` 패널 추가.
- 통장, 카드, 홈택스 매출, 홈택스 매입, 현금영수증, PG 정산 6개 자료 유형별로 상태, 거래 건수, 반영 기간, 다음 확인 작업을 표시.
- 자료가 있으면 `반영됨`, 없으면 `확인 필요`, PG는 선택 자료로 표시.
- 저장된 세무 리포트 상세에도 저장 당시 자료 수집 현황을 표시.
- 신고 패키지 JSON payload에 `dataSourceRows`와 `tables.dataSources` 포함.
- ZIP 패키지에 `csv/data-sources.csv` 추가.
- XLSX 통합 문서에 `자료수집` 시트 추가.
- 패키지 요약 시트에 `확인 필요 자료` 건수 추가.
- README MVP 범위에 자료 수집 현황 확인 항목 추가.

## 검증

- `npm run typecheck` 통과.
- `npm run lint` 통과.
- `npm run verify:mvp` 통과.
- 브라우저 데스크톱 1280px 확인:
  - `자료 수집 현황` 섹션 표시.
  - 자료 유형 6개 행 표시.
  - CSV 다운로드 버튼 표시.
  - 콘솔 에러 없음.
  - 페이지 수평 오버플로우 없음.
- 브라우저 모바일 390px 확인:
  - 섹션과 6개 행 표시.
  - 페이지 수평 오버플로우 없음.
  - 자료 테이블은 내부 스크롤로 처리.

## 남은 확인

- Railway 공개 URL은 이전 확인에서 legacy 정적 페이지가 계속 노출되고 API 라우트가 404였다.
- 저장소 설정과 standalone 빌드는 정상이라 Railway 서비스의 연결 저장소, 브랜치, Root Directory, Start Command, 도메인 매핑을 dashboard에서 확인해야 한다.
