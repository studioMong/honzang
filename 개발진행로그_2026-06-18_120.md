# 개발 진행 로그 120

## 작업 단위
- 설정 화면 작업 실패 메시지 보강 커밋(`f6a77216243981a51304a65902f54f1f6878eb2e`) 이후 공개 Railway 배포 상태를 감사했다.

## 실행 명령
- `RAILWAY_AUDIT_SOFT=1 RAILWAY_EXPECTED_COMMIT=f6a77216243981a51304a65902f54f1f6878eb2e npm run audit:railway`

## 감사 결과
- 공개 주소: `https://honzang-production.up.railway.app`
- 기대 커밋: `f6a77216243981a51304a65902f54f1f6878eb2e`
- `/`: HTTP 200, `text/html; charset=utf-8`, 24489 bytes
- `/api/version`: HTTP 404
- `/api/health`: HTTP 404
- `/manifest.webmanifest`: HTTP 404
- 요약: 5 fail, 0 warn, 0 pass

## 실패 항목
- `Next.js API 라우트 미노출`
- `Legacy 정적 페이지 응답`
- `Healthcheck 미노출`
- `PWA manifest 미노출`
- `보안 헤더 누락`

## 판단
- 최신 커밋은 GitHub `main`에 푸시됐지만 공개 Railway 도메인은 아직 legacy/static 서비스에 연결되어 있다.
- 최신 앱의 API, PWA manifest, 보안 헤더가 공개 URL에 노출되지 않는다.

## 다음 조치
- Railway Dashboard에서 public domain 연결 대상 서비스를 최신 Next.js standalone 서비스로 전환한다.
- 전환 후 `RAILWAY_EXPECTED_COMMIT=$(git rev-parse HEAD) npm run verify:railway`를 실행한다.
