# 개발진행로그 2026-06-18 256

## Railway 배포 감사
- 대상 URL: `https://honzang-production.up.railway.app`
- 기대 커밋: `8132deb69023a495035cdc3043b8dcb328e2b989`
- 실행 명령: `RAILWAY_AUDIT_SOFT=1 RAILWAY_EXPECTED_COMMIT=8132deb69023a495035cdc3043b8dcb328e2b989 npm run audit:railway`

## 결과
- 요약: 5 fail, 0 warn, 0 pass
- `/`: HTTP 200이지만 타이틀이 `혼자장부 프로젝트 정리`인 legacy/static 페이지 응답
- `/api/version`: HTTP 404
- `/api/health`: HTTP 404
- `/manifest.webmanifest`: HTTP 404
- 최신 Next.js 보안 헤더 미반영

## 판단
- 최신 지급명세서 일정 보강 커밋은 GitHub에 푸시됐지만, Railway 공개 도메인은 여전히 최신 Next.js 앱으로 연결되지 않았다.
- 로컬 `npm run verify:mvp`와 브라우저 검증은 통과했으므로, 운영 차단점은 Railway Dashboard의 도메인/서비스 연결 상태다.

## 다음 조치
- `honzang-production.up.railway.app` 도메인의 연결 대상 서비스를 Dockerfile 기반 Next.js 서비스로 전환한다.
- 전환 후 `/api/version`, `/api/health`, `/manifest.webmanifest` 응답을 확인한다.
- 최신 커밋 기준 `RAILWAY_EXPECTED_COMMIT=$(git rev-parse HEAD) npm run verify:railway`를 다시 실행한다.
