# 개발 진행 로그 2026-06-18 61

## 작업 범위
- 백업 복원 dry-run에서 거래의 원본 가져오기 배치 참조가 유효한지 검증하도록 보강했다.

## 구현 내용
- `src/app/api/backups/restore/route.ts`
  - `validateBackupTransactions`가 전체 백업 객체를 받아 거래와 가져오기 배치 목록을 함께 확인하도록 변경했다.
  - 거래의 `importBatchId`가 백업 가져오기 목록에 없으면 `INVALID_BACKUP_TRANSACTIONS` 이슈로 반환한다.
  - 거래의 `sourceType`과 연결된 가져오기 배치의 `sourceType`이 다르면 이슈로 반환한다.
  - 실제 복원 단계에서 원본 CSV 연결이 조용히 `null`로 떨어지는 상황을 dry-run에서 미리 차단한다.

## 테스트 보강
- `scripts/verify-backup-restore.mjs`
  - 거래가 존재하지 않는 가져오기 배치 ID를 참조하는 백업 JSON을 HTTP 400과 `INVALID_BACKUP_TRANSACTIONS`로 거절하는지 확인한다.

## 검증
- `npm run typecheck`
- `npm run lint`
- `git diff --check`
- `npm run verify:mvp`

## 결과
- 전체 MVP 검증이 통과했다.
- 공개 Railway 도메인의 legacy/static 연결 문제는 별도 감사 단계에서 계속 확인해야 한다.
