# 개발 진행 로그 - 2026-06-18 32

## 목표
- CSV 오류 상세 표시 커밋 `d0fb877c3a6c1aff4fb65495e1ae913c3c56042b` 기준으로 Railway 공개 주소 배포 상태를 재감사한다.

## 실행
- `RAILWAY_AUDIT_SOFT=1 RAILWAY_EXPECTED_COMMIT=d0fb877c3a6c1aff4fb65495e1ae913c3c56042b npm run audit:railway`

## 결과
- 감사 대상: `https://honzang-production.up.railway.app`
- `/`: HTTP 200, `text/html; charset=utf-8`, 24489 bytes
- `/api/version`: HTTP 404
- `/api/health`: HTTP 404
- `/manifest.webmanifest`: HTTP 404
- 요약: 4 fail, 0 warn, 0 pass

## 판단
- 공개 도메인은 최신 Next.js 앱이 아니라 기존 legacy/static 응답을 계속 반환한다.
- 앱의 API 라우트와 PWA manifest가 공개 URL에 연결되지 않았다.
- 로컬 `npm run verify:mvp`는 통과했으므로, 남은 배포 문제는 Railway Dashboard의 public domain 서비스 연결 문제로 판단한다.

## 다음 조치
- Railway Dashboard에서 `honzang-production.up.railway.app` public domain 연결 대상 서비스를 최신 Next.js 서비스로 변경한다.
- 최신 서비스의 Healthcheck Path가 `/api/health`인지 확인한다.
- 연결 수정 후 감사 스크립트를 다시 실행한다.
