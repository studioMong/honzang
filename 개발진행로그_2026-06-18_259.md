# 개발진행로그 2026-06-18 259

## 작업 범위
- 신고 일정 계산 로직을 `src/lib/filing-schedule.ts` 순수 모듈로 분리했다.
- `부가세 예정/확정` 회차 계산과 지급명세서/간이지급명세서 기한 계산을 전용 함수로 고정했다.
- `scripts/verify-filing-schedules.ts`를 추가해 부가세 1기 예정/확정, 2기 예정/확정과 지급명세서 기한을 직접 검증한다.
- `npm run verify:filing-schedules`를 추가하고 `npm run verify:mvp` 체인에 포함했다.
- README에 신고 일정 계산 검증 명령을 추가했다.

## 변경 파일
- `src/lib/filing-schedule.ts`
- `scripts/verify-filing-schedules.ts`
- `src/components/app-workspace.tsx`
- `scripts/verify-mvp.mjs`
- `package.json`
- `README.md`

## 검증
- `npm run verify:filing-schedules`: 통과
- `npm run typecheck`: 통과
- `npm run lint`: 통과
- `git diff --check`: 통과
- `npm run verify:mvp`: 통과

## DB 검증
- 현재 셸에 `DATABASE_URL`이 없어 `npm run verify:db-workflow`는 실행하지 않았다.

## 메모
- 기존 화면 스모크는 샘플 데이터 기준 한 기간만 확인했지만, 새 검증은 월별 회차와 다음 연도 기한 경계까지 직접 확인한다.
- 일정 계산을 컴포넌트 밖으로 분리해 향후 법령/신고 기준 변경 시 수정 범위를 줄였다.
