# 개발 진행 로그 176

## 작업 단위
- 대시보드에서 신고 준비에 필요한 자료 수집 상태를 바로 볼 수 있게 했다.
- 코드 커밋(`2f8ada1d4018b51fde12760ac2be0eb815053e0a`) 이후 공개 Railway 배포 상태를 다시 감사했다.

## 구현 내용
- `src/components/app-workspace.tsx` 대시보드에 `자료 수집 현황` 패널을 추가했다.
- 최근 월 기준으로 통장, 카드, 홈택스 매출, 홈택스 매입, 현금영수증, PG 정산 상태를 보여준다.
- 필수 자료 완료 수를 `완료/필수` 형태로 보여준다.
- 각 자료 행에서 반영 건수, 기간, 다음 확인 작업을 바로 볼 수 있게 했다.
- 자료별 버튼은 업로드 화면으로 이동한다.
- `scripts/smoke-production.mjs`의 루트 화면 검증에 `자료 수집 현황`, `필수 자료`, `통장`, `홈택스 매출` 문구를 추가했다.

## 검증
- `npm run typecheck`: 통과
- `npm run lint`: 통과
- `git diff --check`: 통과
- `npm run verify:mvp`: 통과
- `DATABASE_URL`: 현재 셸에 없어 `npm run verify:db-workflow`는 실행하지 못했다.

## 브라우저 확인
- 로컬 production 서버: `http://127.0.0.1:3121/`
- 확인 결과:
  - 문서 제목: `혼자장부`
  - `자료 수집 현황`: 표시됨
  - `필수 자료`: 표시됨
  - `통장`: 표시됨
  - `홈택스 매출`: 표시됨
  - `오늘 할 일`: 표시됨
  - 브라우저 콘솔 에러: 0건

## Railway 감사
- 실행 명령: `RAILWAY_AUDIT_SOFT=1 RAILWAY_EXPECTED_COMMIT=2f8ada1d4018b51fde12760ac2be0eb815053e0a npm run audit:railway`
- 공개 주소: `https://honzang-production.up.railway.app`
- 기대 커밋: `2f8ada1d4018b51fde12760ac2be0eb815053e0a`
- `/`: HTTP 200, `text/html; charset=utf-8`, 24489 bytes, `server=railway-hikari`, `x-railway-request-id=-8r9nvlgSYyxewWa0ubPiw`
- 루트 미리보기: `<title>혼자장부 프로젝트 정리</title>`
- `/api/version`: HTTP 404, `server=railway-hikari`, `x-railway-request-id=YIat--iqSmKEkvn7AQeqjw`
- `/api/health`: HTTP 404, `server=railway-hikari`, `x-railway-request-id=MEyo5dqwTGaDvBxGDcO5xA`
- `/manifest.webmanifest`: HTTP 404, `server=railway-hikari`, `x-railway-request-id=nIbJxpObTwKpnE3yoB_USg`
- 요약: 5 fail, 0 warn, 0 pass

## 판단
- 최신 커밋은 GitHub `main`에 푸시됐지만 공개 Railway 도메인은 여전히 이전 정적 페이지 서비스에 연결되어 있다.
- 첫 화면에서 어떤 자료가 들어왔고 어떤 신고 자료가 빠졌는지 더 빨리 판단할 수 있게 됐다.
- 로컬 production 빌드와 브라우저 렌더링 검증은 정상이다.

## 다음 조치
- Railway Dashboard에서 `honzang-production.up.railway.app` public domain을 최신 Next.js standalone 앱 서비스로 전환한다.
- 전환 후 `RAILWAY_EXPECTED_COMMIT=$(git rev-parse HEAD) npm run verify:railway`를 실행한다.
- 다음 개발 단위에서는 자료 수집 패널의 상태를 실제 업로드 이력과 더 직접적으로 연결할지 검토한다.
