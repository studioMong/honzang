# 개발 진행 로그 2026-06-18 45

## 작업 범위
- 분개 생성 API의 라인 금액 검증을 강화했다.
- 각 분개 라인은 차변 또는 대변 중 한쪽만 0보다 커야 하도록 했다.
- 양쪽 금액이 모두 있거나 양쪽이 모두 0인 라인은 `INVALID_JOURNAL_LINES` 코드로 거절한다.
- production smoke 테스트에 잘못된 분개 라인 거절 케이스를 추가했다.

## 변경 파일
- `src/app/api/journals/route.ts`
- `scripts/smoke-production.mjs`

## 검증
- `npm run typecheck` 통과
- `npm run lint` 통과
- `git diff --check` 통과
- `npm run verify:mvp` 통과

## 메모
- 전체 차변/대변 합계가 맞더라도 라인 자체가 회계적으로 유효하지 않으면 저장하지 않는다.
- 자동분개 초안 외 API 직접 호출 경로에서도 원장 품질을 보장한다.
