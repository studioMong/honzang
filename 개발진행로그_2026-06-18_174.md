# 개발 진행 로그 174

## 작업 단위
- 파일/요청 한도 정책을 README에 문서화했다.
- 문서 커밋(`3588a1e8be0f4f5bf50f329333dc3b4cb9d3be58`) 이후 공개 Railway 배포 상태를 다시 감사했다.

## 구현 내용
- README에 `파일 및 요청 한도` 섹션을 추가했다.
- 원본 CSV 2MB, CSV 가져오기 요청 5MB, DB 보관 증빙 750KB, 백업 JSON 복원 요청 25MB, 일반 JSON API 요청 750KB 한도를 명시했다.
- 더 큰 원본, 증빙, 백업은 Postgres 직접 보관이 아니라 오브젝트 스토리지와 스트리밍 업로드로 분리해야 한다는 운영 판단을 기록했다.

## 검증
- `git diff --check`: 통과
- 문서 변경만 포함되어 `npm run verify:mvp`는 다시 실행하지 않았다.
- 직전 코드 커밋(`afcc2696f89f83fd995899039ab397024e8d70c8`)에서 `npm run typecheck`, `npm run lint`, `npm run verify:mvp`가 통과했다.

## Railway 감사
- 실행 명령: `RAILWAY_AUDIT_SOFT=1 RAILWAY_EXPECTED_COMMIT=3588a1e8be0f4f5bf50f329333dc3b4cb9d3be58 npm run audit:railway`
- 공개 주소: `https://honzang-production.up.railway.app`
- 기대 커밋: `3588a1e8be0f4f5bf50f329333dc3b4cb9d3be58`
- `/`: HTTP 200, `text/html; charset=utf-8`, 24489 bytes, `server=railway-hikari`, `x-railway-request-id=29BLrGsrR5GadD0HVOLIQQ`
- 루트 미리보기: `<title>혼자장부 프로젝트 정리</title>`
- `/api/version`: HTTP 404, `server=railway-hikari`, `x-railway-request-id=QL4Lz-G9SwCE0RAuAQeqjw`
- `/api/health`: HTTP 404, `server=railway-hikari`, `x-railway-request-id=_2RI1CAlTjC1shttacI7Nw`
- `/manifest.webmanifest`: HTTP 404, `server=railway-hikari`, `x-railway-request-id=qZegQDztRQKFaIoMAXC71g`
- 요약: 5 fail, 0 warn, 0 pass

## 판단
- 최신 문서 커밋은 GitHub `main`에 푸시됐지만 공개 Railway 도메인은 여전히 이전 정적 페이지 서비스에 연결되어 있다.
- 파일/요청 한도 정책은 코드와 운영 문서 모두에 반영됐다.

## 다음 조치
- Railway Dashboard에서 `honzang-production.up.railway.app` public domain을 최신 Next.js standalone 앱 서비스로 전환한다.
- 전환 후 `RAILWAY_EXPECTED_COMMIT=$(git rev-parse HEAD) npm run verify:railway`를 실행한다.
- 다음 개발 단위에서는 사용자가 신고 자료를 처음 넣을 때 필요한 필수 입력/자료 상태가 충분히 빠르게 파악되는지 점검한다.
