# 개발 진행 로그 136

## 작업 단위
- 리포트 기간 겹침 마감 가드 커밋(`bde4269e428d4ea2f70092f5c807a6553a6e805f`) 이후 공개 Railway 배포 상태를 감사했다.

## 실행 명령
- `RAILWAY_AUDIT_SOFT=1 RAILWAY_EXPECTED_COMMIT=bde4269e428d4ea2f70092f5c807a6553a6e805f npm run audit:railway`

## 감사 결과
- 공개 주소: `https://honzang-production.up.railway.app`
- 기대 커밋: `bde4269e428d4ea2f70092f5c807a6553a6e805f`
- `/`: HTTP 200, `text/html; charset=utf-8`, 24489 bytes
- `/api/version`: HTTP 404
- `/api/health`: HTTP 404
- `/manifest.webmanifest`: HTTP 404
- 요약: 5 fail, 0 warn, 0 pass

## 실패 항목
- `Next.js API 라우트 미노출`
- `Legacy 정적 페이지 응답`
- `Healthcheck 미노출`
- `PWA manifest 미노출`
- `보안 헤더 누락`

## 판단
- 최신 커밋은 GitHub `main`에 푸시됐지만 공개 Railway 도메인은 아직 최신 Next.js standalone 서비스를 바라보지 않는다.
- 코드 측 API 라우트와 manifest는 로컬 standalone 빌드에서 검증됐으므로, 공개 도메인의 연결 대상 서비스 전환이 남은 배포 차단 요소다.

## 다음 조치
- Railway Dashboard에서 public domain 연결 대상 서비스를 최신 Next.js standalone 서비스로 전환한다.
- 전환 후 `RAILWAY_EXPECTED_COMMIT=$(git rev-parse HEAD) npm run verify:railway`를 실행한다.
