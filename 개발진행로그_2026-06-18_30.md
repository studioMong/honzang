# 개발 진행 로그 - 2026-06-18 30

## 목표
- CSV 행 검증 커밋 `f5a97dbf6d47971911bf2bbc249169813b7e8fe0` 기준으로 Railway 공개 주소 배포 상태를 재감사한다.

## 실행
- `RAILWAY_AUDIT_SOFT=1 RAILWAY_EXPECTED_COMMIT=f5a97dbf6d47971911bf2bbc249169813b7e8fe0 npm run audit:railway`

## 결과
- 감사 대상: `https://honzang-production.up.railway.app`
- `/`: HTTP 200, `text/html; charset=utf-8`, 24489 bytes
- `/api/version`: HTTP 404
- `/api/health`: HTTP 404
- `/manifest.webmanifest`: HTTP 404
- 요약: 4 fail, 0 warn, 0 pass

## 판단
- 공개 도메인은 최신 Next.js 앱이 아니라 기존 legacy/static 응답을 계속 반환한다.
- `/api/version`, `/api/health`, `/manifest.webmanifest`가 모두 404이므로 애플리케이션 라우트가 공개 URL에 연결되지 않았다.
- 코드베이스 기준 `npm run verify:mvp`는 통과했으므로 앱 산출물 자체보다 Railway public domain 연결/서비스 선택 문제로 판단한다.

## 다음 조치
- Railway Dashboard에서 현재 public domain이 연결된 서비스를 확인하고 최신 Next.js 서비스로 옮긴다.
- 최신 서비스의 start command와 healthcheck path가 각각 standalone 실행, `/api/health`인지 확인한다.
- 도메인 연결 수정 뒤 감사 스크립트를 다시 실행한다.
