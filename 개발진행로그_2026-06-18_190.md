# 개발 진행 로그 190

## 작업 시각
- 2026-06-18

## 작업 주제
- 마감 스냅샷 API smoke 구조 검증 보강

## 변경 파일
- `scripts/smoke-production.mjs`

## 변경 내용
- `/api/closing-periods` smoke 검증을 단순 배열 확인에서 구조 확인으로 강화했다.
- sample 모드에서는 `2026-05` 마감 스냅샷이 포함되는지 확인한다.
- sample 마감 스냅샷의 `summaryPayload.report.filingInputSummaryRows` 안에 `과세 매출 공급가액` 행이 포함되는지 확인한다.

## 검증 결과
- `npm run typecheck`: 통과
- `npm run lint`: 통과
- `git diff --check`: 통과
- `npm run verify:mvp`: 통과

## 실행하지 못한 검증
- `npm run verify:db-workflow`
  - 현재 쉘 환경에 `DATABASE_URL`이 없어 실제 Postgres 저장 흐름 검증은 실행하지 않았다.

## 다음 작업
- 변경 커밋 및 원격 푸시
- 푸시 후 Railway 공개 도메인 감사 재실행
- 신고/마감 확정본 검증 범위를 계속 확장
