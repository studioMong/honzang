# 개발진행로그 73

## 작업 시각

- 2026-06-17 23:20 KST

## 목표

- Railway 공개 URL이 최신 Next.js 서비스가 아닌 상태를 대시보드 접근 없이도 더 구체적으로 분류할 수 있게 한다.
- 사용자가 Railway Dashboard에서 확인해야 할 항목을 진단 출력만 보고 바로 대조할 수 있게 한다.

## 작업 내용

- `scripts/audit-railway-deployment.mjs`를 추가했다.
- `package.json`에 `npm run audit:railway` 명령을 추가했다.
- `verify:deployment-config`가 Railway 감사 스크립트 존재와 npm 스크립트 연결을 확인하도록 확장했다.
- README에 `audit:railway` 사용법과 `RAILWAY_AUDIT_SOFT=1` soft mode를 추가했다.

## 감사 스크립트가 구분하는 상태

- Next.js API 라우트 미노출
- legacy 정적 페이지 응답
- 배포 커밋 불일치
- Railway 커밋 메타데이터 누락
- Postgres 미연결
- PWA manifest 미노출

## 검증

- `npm run verify:deployment-config` 통과
- `npm run typecheck` 통과
- `npm run lint` 통과
- `RAILWAY_AUDIT_SOFT=1 npm run audit:railway` 실행 확인
- `npm run verify:mvp` 통과

## 현재 Railway 감사 결과

- 공개 URL: `https://honzang-production.up.railway.app`
- 기대 커밋: `56dc07dec1128534e38972b4c28d57970236b31b`
- 결과 요약: `4 fail, 0 warn, 0 pass`
- 실패 항목:
  - Next.js API 라우트 미노출
  - Legacy 정적 페이지 응답
  - Healthcheck 미노출
  - PWA manifest 미노출

## 판단

- 로컬 production standalone 빌드와 MVP 검증은 정상이다.
- 공개 URL은 여전히 최신 Next.js 서비스가 아니라 legacy/static 서비스에 연결된 상태로 분류된다.
- Railway Dashboard에서 public domain을 최신 서비스로 옮기는 외부 조치가 필요하다.
