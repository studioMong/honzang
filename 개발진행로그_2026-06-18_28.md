# 개발 진행 로그 - 2026-06-18 28

## 목표
- CSV 매핑 서버 검증 커밋 `eb4eee4c0e18b1b69142a34bcd1ec068e2f824c9` 기준으로 Railway 공개 주소 배포 상태를 재감사한다.

## 실행
- `RAILWAY_AUDIT_SOFT=1 RAILWAY_EXPECTED_COMMIT=eb4eee4c0e18b1b69142a34bcd1ec068e2f824c9 npm run audit:railway`

## 결과
- 감사 대상: `https://honzang-production.up.railway.app`
- `/`: HTTP 200, `text/html; charset=utf-8`, 24489 bytes
- `/api/version`: HTTP 404
- `/api/health`: HTTP 404
- `/manifest.webmanifest`: HTTP 404
- 요약: 4 fail, 0 warn, 0 pass

## 판단
- 최신 커밋은 GitHub 원격 `main`에 푸시됐지만, 공개 Railway 도메인은 아직 최신 Next.js 앱을 서빙하지 않는다.
- 루트 페이지는 legacy/static HTML 응답으로 판단되며, API 라우트와 PWA manifest가 공개되지 않는다.
- 애플리케이션 로컬 검증은 통과했으므로 남은 배포 완료 조건은 Railway Dashboard에서 public domain 연결 대상을 최신 Next.js 서비스로 수정하는 것이다.

## 다음 조치
- Railway Dashboard에서 `honzang-production.up.railway.app` 도메인이 연결된 서비스를 확인한다.
- 최신 Next.js 서비스의 Healthcheck Path를 `/api/health`로 설정하고 public domain을 해당 서비스에 연결한다.
- 연결 수정 뒤 `/api/version`, `/api/health`, `/manifest.webmanifest`를 다시 감사한다.
