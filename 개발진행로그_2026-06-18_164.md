# 개발 진행 로그 164

## 작업 단위
- Railway 공개 도메인 감사 스크립트가 응답 헤더와 legacy 루트 미리보기를 출력하도록 보강했다.
- 코드 커밋(`1c273549d4968a254ea0dd76ea5845c674018626`) 이후 공개 Railway 배포 상태를 다시 감사했다.

## 구현 내용
- `scripts/audit-railway-deployment.mjs`에서 각 경로 응답의 `server`, `via`, `x-railway-request-id`를 출력하게 했다.
- 루트 페이지가 legacy 정적 페이지로 판별되면 HTML 미리보기를 함께 출력하게 했다.

## 검증
- `npm run typecheck`: 통과
- `npm run lint`: 통과
- `git diff --check`: 통과
- `npm run verify:mvp`: 통과
- `DATABASE_URL`: 현재 셸에 없어 `npm run verify:db-workflow`는 실행하지 못했다.

## Railway 감사
- 실행 명령: `RAILWAY_AUDIT_SOFT=1 RAILWAY_EXPECTED_COMMIT=1c273549d4968a254ea0dd76ea5845c674018626 npm run audit:railway`
- 공개 주소: `https://honzang-production.up.railway.app`
- 기대 커밋: `1c273549d4968a254ea0dd76ea5845c674018626`
- `/`: HTTP 200, `text/html; charset=utf-8`, 24489 bytes, `server=railway-hikari`, `x-railway-request-id=tQ8FCZnWTQu6jTZdipRofQ`
- 루트 미리보기: `<title>혼자장부 프로젝트 정리</title>`
- `/api/version`: HTTP 404, `server=railway-hikari`, `x-railway-request-id=W-zE-6onREWu6vfFAQeqjw`
- `/api/health`: HTTP 404, `server=railway-hikari`, `x-railway-request-id=RrlX5urFSfqyuLeIAQeqjw`
- `/manifest.webmanifest`: HTTP 404, `server=railway-hikari`, `x-railway-request-id=w6akLyAoTeqC_iu3-8Y8hA`
- 요약: 5 fail, 0 warn, 0 pass

## 판단
- 공개 도메인은 Railway 인프라에서 응답하지만 최신 Next.js standalone 앱이 아니라 이전 정적 페이지 서비스가 응답한다.
- `/api/version`, `/api/health`, `/manifest.webmanifest`가 계속 404이므로 코드 문제가 아니라 Railway public domain 연결 대상 문제로 본다.

## 다음 조치
- Railway Dashboard에서 `honzang-production.up.railway.app` public domain이 붙은 서비스를 확인한다.
- 해당 도메인을 최신 Next.js standalone 앱 서비스로 이동하거나 기존 static/legacy 서비스의 public domain 연결을 제거한다.
- 전환 후 `RAILWAY_EXPECTED_COMMIT=$(git rev-parse HEAD) npm run verify:railway`를 실행한다.
