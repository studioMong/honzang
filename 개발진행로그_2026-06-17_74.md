# 개발진행로그 74

## 작업 시각

- 2026-06-17 23:21 KST

## 확인 대상

- 커밋: `b52b1b6` (`Add Railway deployment audit`)
- 공개 URL: `https://honzang-production.up.railway.app`
- 검증 명령:
  - `RAILWAY_EXPECTED_COMMIT=b52b1b6 npm run verify:railway`
  - `RAILWAY_AUDIT_SOFT=1 RAILWAY_EXPECTED_COMMIT=b52b1b6 npm run audit:railway`

## 결과

- Railway 공개 URL 검증 실패.
- `/`는 HTTP 200이지만 legacy 정적 프로젝트 요약 페이지로 응답.
- `/api/version`은 HTTP 404.
- `/api/health`는 HTTP 404.
- `/manifest.webmanifest`는 HTTP 404.

## 감사 스크립트 분류

- 결과 요약: `4 fail, 0 warn, 0 pass`
- 실패 항목:
  - Next.js API 라우트 미노출
  - Legacy 정적 페이지 응답
  - Healthcheck 미노출
  - PWA manifest 미노출

## 판단

- `audit:railway`는 현재 공개 URL 실패를 도메인/서비스 오연결로 분류한다.
- 저장소의 Dockerfile, Railway config, standalone build, MVP 검증은 정상이다.
- 공개 URL이 최신 Next.js 서비스로 연결되지 않는 외부 Railway Dashboard 설정 문제가 계속 남아 있다.

## 필요한 외부 확인

- `honzang-production.up.railway.app` public domain이 최신 Next.js 서비스에 연결되어 있는지 확인.
- 기존 legacy/static 서비스에 도메인이 남아 있다면 해당 도메인을 제거하거나 최신 서비스로 재연결.
- 최신 서비스의 GitHub branch, Root Directory, Dockerfile build, `DATABASE_URL`, `npm run db:deploy`, `npm run start` 설정 확인.
