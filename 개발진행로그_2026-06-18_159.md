# 개발 진행 로그 159

## 작업 단위
- CSV 가져오기 API의 날짜 검증을 공통 `parseStrictDate` 기준으로 통합했다.
- `MM/DD/YYYY` 같은 모호한 미국식 날짜가 서버 검증을 통과하지 않도록 production smoke 검증을 추가했다.

## 변경 파일
- `src/app/api/imports/route.ts`
- `scripts/smoke-production.mjs`

## 검증
- `npm run typecheck`: 통과
- `npm run lint`: 통과
- `git diff --check`: 통과
- `npm run verify:mvp`: 통과

## DB 검증 상태
- 현재 셸에 `DATABASE_URL`이 없어 `npm run verify:db-workflow`는 실행하지 못했다.

## 비고
- 서버 CSV 검증과 클라이언트 미리보기 날짜 정규화가 같은 방향으로 동작하도록 맞췄다.
