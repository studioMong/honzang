# 개발 진행 로그 2026-06-18 63

## 작업 범위
- 백업 복원 dry-run에서 거래의 추천/확정 계정과목 참조가 유효한지 검증하도록 보강했다.

## 구현 내용
- `src/app/api/backups/restore/route.ts`
  - `validateBackupTransactions`가 기본 계정과 백업 계정 목록을 합쳐 유효 계정 코드 집합을 만든다.
  - 거래의 `suggestedAccount.code`가 유효 계정 목록에 없으면 `INVALID_BACKUP_TRANSACTIONS` 이슈로 반환한다.
  - 거래의 `confirmedAccount.code`가 유효 계정 목록에 없으면 `INVALID_BACKUP_TRANSACTIONS` 이슈로 반환한다.
  - 실제 복원 단계에서 추천/확정 계정 연결이 조용히 `null`로 떨어지는 상황을 dry-run에서 미리 차단한다.

## 테스트 보강
- `scripts/verify-backup-restore.mjs`
  - 거래가 존재하지 않는 확정 계정 코드를 참조하는 백업 JSON을 HTTP 400과 `INVALID_BACKUP_TRANSACTIONS`로 거절하는지 확인한다.

## 검증
- `npm run typecheck`
- `npm run lint`
- `git diff --check`
- `npm run verify:mvp`

## 결과
- 전체 MVP 검증이 통과했다.
- 공개 Railway 도메인의 legacy/static 연결 문제는 별도 감사 단계에서 계속 확인해야 한다.
