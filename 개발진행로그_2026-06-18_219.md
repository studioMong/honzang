# 개발 진행 로그 219

## 작업 주제
- 커밋 `5835ae4005bff7d12844209cfa4029d7d2dacb9b` 푸시 후 Railway 공개 도메인 감사.

## 감사 명령
- `RAILWAY_AUDIT_SOFT=1 RAILWAY_EXPECTED_COMMIT=5835ae4005bff7d12844209cfa4029d7d2dacb9b npm run audit:railway`

## 결과
- 대상 URL: `https://honzang-production.up.railway.app`
- 요약: `5 fail, 0 warn, 0 pass`
- soft mode로 실행되어 명령 자체는 성공 종료했다.

## 응답 상세
- `/`
  - HTTP 200
  - `content-type`: `text/html; charset=utf-8`
  - 크기: `24489 bytes`
  - 서버: `railway-hikari`
  - `x-railway-request-id`: `YJhOkx2vQvajnEiaacI7Nw`
  - preview에 `<title>혼자장부 프로젝트 정리</title>`가 남아 있어 legacy 정적 페이지로 판단된다.
- `/api/version`
  - HTTP 404
  - `x-railway-request-id`: `m37wPp1kSs2KaJaVacI7Nw`
- `/api/health`
  - HTTP 404
  - `x-railway-request-id`: `gyFhgEmpTfuOWWbwVOLIQQ`
- `/manifest.webmanifest`
  - HTTP 404
  - `x-railway-request-id`: `5EYoT_naT--2ERpPacI7Nw`

## 실패 항목
- Next.js API 라우트 미노출: `/api/version`이 404.
- Legacy 정적 페이지 응답: 루트 페이지가 이전 프로젝트 요약 HTML.
- Healthcheck 미노출: `/api/health`가 404.
- PWA manifest 미노출: `/manifest.webmanifest`가 404.
- 보안 헤더 누락: 최신 Next.js 앱의 보안 헤더가 공개 URL에 반영되지 않음.

## 판단
- GitHub `main`에는 healthcheck DB 스키마 검증 커밋까지 반영되었다.
- Railway public domain은 여전히 최신 Next.js 서비스가 아니라 legacy/static 서비스에 연결된 상태다.
- Railway Dashboard에서 Public Networking 도메인 연결 대상 서비스를 최신 Next.js 앱으로 전환해야 한다.
