# 개발진행로그 64

## 작업 시각

- 2026-06-17 23:00 KST

## 확인 대상

- 커밋: `92301da` (`Add filing readiness check`)
- 공개 URL: `https://honzang-production.up.railway.app`
- 검증 명령: `RAILWAY_EXPECTED_COMMIT=92301da npm run verify:railway`

## 결과

- Railway 공개 URL 검증 실패.
- `/`는 HTTP 200이지만 legacy 정적 프로젝트 요약 페이지로 응답.
- `/api/version`은 HTTP 404.
- `/api/health`는 HTTP 404.
- `/manifest.webmanifest`는 HTTP 404.

## 판단

- 로컬/저장소 기준 `npm run verify:mvp`는 통과했다.
- 최신 커밋의 Next.js API 라우트와 PWA manifest는 빌드 산출물에 존재한다.
- 따라서 공개 도메인이 최신 Next.js standalone 서비스가 아니라 이전 정적 서비스 또는 다른 Railway 서비스에 연결된 상태로 보는 것이 맞다.

## 필요한 외부 확인

- Railway Dashboard에서 `honzang-production.up.railway.app` 도메인이 최신 GitHub `main` 브랜치 서비스에 연결되어 있는지 확인.
- 서비스 Root Directory가 저장소 루트인지 확인.
- Dockerfile 빌드가 감지되는지 확인.
- Pre-deploy가 `npm run db:deploy`, Start가 `npm run start`인지 확인.
- `DATABASE_URL`이 Railway Postgres 참조 변수로 연결되어 있는지 확인.
