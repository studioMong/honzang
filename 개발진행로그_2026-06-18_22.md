# 개발 진행 로그 - 2026-06-18 22

## 목표
- CSV 헤더별 템플릿 보존 커밋 `59ead5423f93efe075439819b4248f5df4847f4c` 기준으로 Railway 공개 주소 배포 상태를 재감사한다.

## 실행
- `RAILWAY_AUDIT_SOFT=1 RAILWAY_EXPECTED_COMMIT=59ead5423f93efe075439819b4248f5df4847f4c npm run audit:railway`

## 결과
- 감사 대상: `https://honzang-production.up.railway.app`
- `/`: HTTP 200, `text/html; charset=utf-8`, 24489 bytes
- `/api/version`: HTTP 404
- `/api/health`: HTTP 404
- `/manifest.webmanifest`: HTTP 404
- 요약: 4 fail, 0 warn, 0 pass

## 판단
- 공개 도메인은 아직 최신 Next.js 앱을 서빙하지 않는다.
- 앱 서버 API 라우트와 PWA manifest가 노출되지 않아, 현재 public domain이 최신 Next.js 서비스가 아닌 legacy/static 서비스에 연결된 상태로 판단한다.
- 로컬 빌드와 MVP 검증은 통과했으므로 남은 배포 이슈는 Railway Dashboard의 도메인/서비스 연결 확인이 필요하다.

## 다음 조치
- Railway Dashboard에서 `honzang-production.up.railway.app` public domain 연결 대상 서비스를 최신 Next.js 서비스로 변경한다.
- Healthcheck Path를 `/api/health`로 유지한다.
- 연결 수정 후 `npm run audit:railway`와 `npm run smoke:prod`를 다시 실행한다.
