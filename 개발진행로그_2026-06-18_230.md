# 개발 진행 로그 230

## 작업 시각
- 2026-06-18

## 목표
- 자동분개 화면과 검증 스크립트의 분개 균형 판정을 API와 같은 1/100 단위 기준으로 맞춘다.
- 차변/대변이 원 단위 반올림으로는 같아 보여도 실제 Decimal 금액 기준으로 다른 경우를 “정상 초안”으로 표시하지 않게 한다.

## 변경 내용
- `src/lib/money.ts` 추가
  - `MAX_DECIMAL_14_2_AMOUNT`
  - `moneyToMinorUnits`
  - `minorUnitsToMoney`
  - `hasAtMostTwoDecimalPlaces`
- `src/lib/server/money-validation.ts`
  - 서버 전용 검증 파일 안에 있던 순수 금액 단위 변환 로직을 공용 유틸에서 재사용하도록 정리
  - 기존 export 호환성은 유지
- `src/components/app-workspace.tsx`
  - 자동분개 초안의 균형 판정을 `Math.round` 대신 `moneyToMinorUnits` 합계 비교로 변경
- `scripts/verify-db-workflow.ts`
  - DB 워크플로 검증의 분개 균형 헬퍼를 1/100 단위 비교로 변경
- `scripts/verify-sample-csvs.ts`
  - 샘플 CSV 검증의 분개 균형 헬퍼를 1/100 단위 비교로 변경

## 검증 결과
- `npm run typecheck` 통과
- `npm run lint` 통과
- `git diff --check` 통과
- `npm run verify:samples`
  - 최초 샌드박스 실행은 `tsx` IPC 파이프 생성 `EPERM`으로 실패
  - 권한 상승 재실행에서 통과
- `npm run verify:mvp` 통과
  - 배포 설정, 파일 한도, 자료 수집, 과금 추정, 파일 암호화, DB 스키마 계약, 마감 잠금, 제품 경계, 타입체크, 린트, CSV 샘플, 내보내기, 빌드, PWA, 접근 제어, 접근 감사, 백업 복원, 프로덕션 스모크 모두 통과
- `printenv DATABASE_URL`
  - 현재 로컬 환경에 값이 없어 `verify:db-workflow`는 실행하지 않음

## 판단
- API 저장 검증, 화면의 정상/검토 분류, 검증 스크립트의 균형 판정 기준이 같은 1/100 단위로 맞춰졌다.
- 사용자가 화면에서 “정상 초안”으로 본 분개가 API 저장 단계에서 정밀도 불일치로 거부될 가능성을 줄였다.
