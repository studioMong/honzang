# 개발 진행 로그 191

## 작업 시각
- 2026-06-18

## 작업 주제
- 커밋 `c808d349323007364abb5ffda643e631a3c919e1` 푸시 후 Railway 공개 배포 감사

## 원격 반영
- 커밋: `c808d34 Verify closing snapshot smoke payload`
- 원격 저장소: `git@github.com:studioMong/honzang.git`
- 브랜치: `main`
- 푸시 완료

## Railway 감사 명령
```bash
RAILWAY_AUDIT_SOFT=1 RAILWAY_EXPECTED_COMMIT=c808d349323007364abb5ffda643e631a3c919e1 npm run audit:railway
```

## Railway 감사 결과
- 대상 URL: `https://honzang-production.up.railway.app`
- `/`
  - HTTP 200
  - `content-type`: `text/html; charset=utf-8`
  - 응답 크기: 24489 bytes
  - `server`: `railway-hikari`
  - `x-railway-request-id`: `52CeZpOwRVKmSpReJH0Vcg`
  - 루트 preview: `<title>혼자장부 프로젝트 정리</title>`
- `/api/version`
  - HTTP 404
  - `x-railway-request-id`: `6GLjhEmESASstWhzipRofQ`
- `/api/health`
  - HTTP 404
  - `x-railway-request-id`: `BWqBHW1tRlqAtz1lipRofQ`
- `/manifest.webmanifest`
  - HTTP 404
  - `x-railway-request-id`: `XaEgeW0ITN2aU_ZWDcO5xA`

## 판정
- 실패 5건, 경고 0건, 통과 0건
- 공개 Railway 도메인은 최신 Next.js standalone 서비스가 아니라 이전 정적/레거시 서비스에 연결된 상태로 유지된다.
- 최신 커밋은 GitHub `main`에 정상 푸시됐다.

## 필요한 외부 조치
- Railway Dashboard에서 public domain 연결 대상을 최신 Next.js 서비스로 변경해야 한다.
- `/api/health`, `/api/version`, `/manifest.webmanifest`가 200으로 확인되기 전까지 공개 URL 배포 완료로 볼 수 없다.
- 참고 문서: `docs/railway-cutover.md`

## 다음 작업
- 코드 측면 MVP 안정화는 계속 진행
- Railway 도메인 연결은 사용자 Railway Dashboard 권한이 필요한 외부 설정 이슈로 유지
