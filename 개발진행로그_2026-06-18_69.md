# 개발 진행 로그 2026-06-18 69

## 작업 범위
- 백업 복원 dry-run에서 리포트 기간과 마감 기간 데이터의 정합성을 검증하도록 보강했다.

## 구현 내용
- `src/app/api/backups/restore/route.ts`
  - 세무 리포트 `periodStart`가 `periodEnd`보다 늦으면 `INVALID_BACKUP_DATES` 이슈로 반환한다.
  - 마감 `periodStart`가 `periodEnd`보다 늦으면 `INVALID_BACKUP_DATES` 이슈로 반환한다.
  - 마감 `period`의 월 범위와 `periodStart`/`periodEnd`가 맞지 않으면 `INVALID_BACKUP_DATES` 이슈로 반환한다.
  - 실제 복원 단계에서 마감 월과 날짜 범위가 어긋난 데이터가 들어오는 상황을 dry-run에서 미리 차단한다.

## 테스트 보강
- `scripts/verify-backup-restore.mjs`
  - 리포트 기간이 역전된 백업 JSON을 거절하는지 확인한다.
  - 마감 월의 시작/종료일이 해당 월 범위와 다르면 거절하는지 확인한다.

## 검증
- `npm run typecheck`
- `npm run lint`
- `git diff --check`
- `npm run verify:mvp`

## 결과
- 전체 MVP 검증이 통과했다.
- 공개 Railway 도메인의 legacy/static 연결 문제는 별도 감사 단계에서 계속 확인해야 한다.
