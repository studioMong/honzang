# 개발진행로그 2026-06-18 2

## Railway 공개 URL 점검
- 기준 커밋: `df39fd3`
- 점검 명령: `RAILWAY_EXPECTED_COMMIT=df39fd3 npm run audit:railway`
- 대상 URL: `https://honzang-production.up.railway.app`

## 결과
- `/`: HTTP 200, 레거시 정적 HTML 응답.
- `/api/version`: HTTP 404.
- `/api/health`: HTTP 404.
- `/manifest.webmanifest`: HTTP 404.
- 요약: `4 fail, 0 warn, 0 pass`.

## 판단
- GitHub 원격 저장소에는 최신 커밋이 푸시되어 있다.
- 로컬 standalone 빌드와 MVP 검증은 통과한다.
- 공개 도메인은 최신 Next.js 서비스가 아니라 이전 정적/레거시 서비스에 연결되어 있는 상태로 보인다.

## Railway 대시보드에서 확인할 항목
- `honzang-production.up.railway.app` 도메인이 연결된 서비스가 현재 Next.js 앱 서비스인지 확인.
- 해당 서비스의 배포 소스가 `github.com/studioMong/honzang`의 `main` 브랜치인지 확인.
- 배포된 커밋이 `df39fd3` 이상인지 확인.
- 루트 디렉터리가 저장소 루트인지 확인.
- Railway 빌더가 `Dockerfile`을 사용하고 있는지 확인.
- Healthcheck Path가 `/api/health`인지 확인.
- `DATABASE_URL`, `NEXT_PUBLIC_APP_URL`, `HONZANG_ACCESS_CODE` 환경변수가 현재 서비스에 설정되어 있는지 확인.
