# 개발 진행 로그 167

## 작업 단위
- 신고 패키지와 각종 CSV 내보내기에서 스프레드시트 수식 주입을 방어했다.
- 코드 커밋(`ea69af0d90a0b926308ee6b9ac951f0eaa9eba4a`) 이후 공개 Railway 배포 상태를 다시 감사했다.

## 구현 내용
- `src/lib/export-safety.ts`를 추가해 CSV 셀 값이 `=`, `+`, `-`, `@` 또는 공백 뒤 수식 시작 문자로 시작하면 앞에 `'`를 붙이도록 했다.
- `src/components/app-workspace.tsx`의 CSV 생성 함수가 해당 방어 유틸을 거치게 했다.
- 신고 패키지 ZIP 내부 CSV와 개별 CSV 다운로드가 같은 CSV 생성 함수를 쓰므로 함께 보호된다.
- `scripts/verify-sample-csvs.ts`에 수식형 텍스트 방어, 선행 공백 뒤 수식형 텍스트 방어, 숫자 음수 보존 회귀 검증을 추가했다.

## 검증
- `npm run typecheck`: 통과
- `npm run lint`: 통과
- `git diff --check`: 통과
- `npm run verify:mvp`: 통과
- `DATABASE_URL`: 현재 셸에 없어 `npm run verify:db-workflow`는 실행하지 못했다.

## Railway 감사
- 실행 명령: `RAILWAY_AUDIT_SOFT=1 RAILWAY_EXPECTED_COMMIT=ea69af0d90a0b926308ee6b9ac951f0eaa9eba4a npm run audit:railway`
- 공개 주소: `https://honzang-production.up.railway.app`
- 기대 커밋: `ea69af0d90a0b926308ee6b9ac951f0eaa9eba4a`
- `/`: HTTP 200, `text/html; charset=utf-8`, 24489 bytes, `server=railway-hikari`, `x-railway-request-id=P1DgMmqPQsqIWJyaJH0Vcg`
- 루트 미리보기: `<title>혼자장부 프로젝트 정리</title>`
- `/api/version`: HTTP 404, `server=railway-hikari`, `x-railway-request-id=hwCZwdV5RtOwvPkMJH0Vcg`
- `/api/health`: HTTP 404, `server=railway-hikari`, `x-railway-request-id=kJDcCH3HRYeZZ-yUV7rehQ`
- `/manifest.webmanifest`: HTTP 404, `server=railway-hikari`, `x-railway-request-id=EG1rVTbVTRyEG7tTAXC71g`
- 요약: 5 fail, 0 warn, 0 pass

## 판단
- 최신 커밋은 GitHub `main`에 푸시됐지만 공개 Railway 도메인은 여전히 이전 정적 페이지 서비스에 연결되어 있다.
- CSV 내보내기 보안과 로컬 standalone 검증은 완료됐고, 남은 공개 배포 이슈는 Railway Dashboard의 public domain 연결 대상 문제다.

## 다음 조치
- Railway Dashboard에서 `honzang-production.up.railway.app` public domain을 최신 Next.js standalone 앱 서비스로 전환한다.
- 전환 후 `RAILWAY_EXPECTED_COMMIT=$(git rev-parse HEAD) npm run verify:railway`를 실행한다.
