# 개발 진행 로그 2026-06-18 43

## 작업 범위
- 분개 생성 API의 `entryDate` 검증을 강화했다.
- 잘못된 분개일은 `INVALID_JOURNAL_DATE` 코드로 거절하도록 했다.
- sample 모드 응답과 DB 저장 모두 정규화된 분개일을 사용하도록 했다.
- production smoke 테스트에 잘못된 분개일 거절 케이스를 추가했다.

## 변경 파일
- `src/app/api/journals/route.ts`
- `scripts/smoke-production.mjs`

## 검증
- `npm run typecheck` 통과
- `npm run lint` 통과
- `git diff --check` 통과
- `npm run verify:mvp` 통과

## 메모
- 거래일, 증빙 발행일, 백업 날짜에 이어 분개일도 같은 공통 날짜 검증 기준을 사용한다.
- 잘못된 분개일이 마감 잠금 판단이나 DB 저장 단계에서 자동 보정되지 않도록 API 입구에서 차단한다.
