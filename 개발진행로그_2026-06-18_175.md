# 개발 진행 로그 175

## 작업 단위
- 파일/요청 한도 정책이 코드와 README에서 어긋나지 않도록 자동 검증을 추가했다.
- 코드 커밋(`2ae631145661ba45a47a26114d5139cdab01750e`) 이후 공개 Railway 배포 상태를 다시 감사했다.

## 구현 내용
- `scripts/verify-file-limits.mjs`를 추가했다.
- `package.json`에 `npm run verify:limits`를 추가했다.
- `scripts/verify-mvp.mjs`에 `verify:limits` 단계를 연결했다.
- 검증 스크립트는 다음을 확인한다.
  - `src/lib/file-limits.ts`의 파일/요청 한도 상수
  - 서버 validation과 route가 공통 한도를 import하는지
  - 클라이언트 백업 복원이 요청 body 크기를 사전 검사하는지
  - README가 운영 한도 문구를 포함하는지

## 검증
- `npm run verify:limits`: 통과
- `npm run typecheck`: 통과
- `npm run lint`: 통과
- `git diff --check`: 통과
- `npm run verify:mvp`: 통과
- `DATABASE_URL`: 현재 셸에 없어 `npm run verify:db-workflow`는 실행하지 못했다.

## Railway 감사
- 실행 명령: `RAILWAY_AUDIT_SOFT=1 RAILWAY_EXPECTED_COMMIT=2ae631145661ba45a47a26114d5139cdab01750e npm run audit:railway`
- 공개 주소: `https://honzang-production.up.railway.app`
- 기대 커밋: `2ae631145661ba45a47a26114d5139cdab01750e`
- `/`: HTTP 200, `text/html; charset=utf-8`, 24489 bytes, `server=railway-hikari`, `x-railway-request-id=ZefGgDvYQfaButCxVOLIQQ`
- 루트 미리보기: `<title>혼자장부 프로젝트 정리</title>`
- `/api/version`: HTTP 404, `server=railway-hikari`, `x-railway-request-id=n5oU9qmnSMq5bpFkJH0Vcg`
- `/api/health`: HTTP 404, `server=railway-hikari`, `x-railway-request-id=PaMGEa4mTyu4cEoEVOLIQQ`
- `/manifest.webmanifest`: HTTP 404, `server=railway-hikari`, `x-railway-request-id=pA4KHqRGTNi56Psd0ubPiw`
- 요약: 5 fail, 0 warn, 0 pass

## 판단
- 최신 커밋은 GitHub `main`에 푸시됐지만 공개 Railway 도메인은 여전히 이전 정적 페이지 서비스에 연결되어 있다.
- 파일/요청 한도 정책은 코드, README, MVP 검증 루틴에 모두 묶였다.

## 다음 조치
- Railway Dashboard에서 `honzang-production.up.railway.app` public domain을 최신 Next.js standalone 앱 서비스로 전환한다.
- 전환 후 `RAILWAY_EXPECTED_COMMIT=$(git rev-parse HEAD) npm run verify:railway`를 실행한다.
- 다음 개발 단위에서는 첫 사용자가 필수 입력과 자료 업로드 상태를 더 빠르게 판단할 수 있는지 대시보드/설정 흐름을 점검한다.
