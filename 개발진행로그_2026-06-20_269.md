# 개발 진행 로그 269

## 작업 시각

- 2026-06-20

## 목적

- 사용자가 Railway 배포가 된 것 같다고 알려준 뒤, 공개 URL이 최신 Next.js 앱과 Railway Postgres를 정상적으로 바라보는지 확인한다.

## 기준 커밋

- 검증 기준 커밋: `e1fb4a8eee7fd5a4edf895b76cf0b4305880a34b`
- 원격 저장소: `git@github.com:studioMong/honzang.git`
- 공개 URL: `https://honzang-production.up.railway.app`

## 실행한 검증

```bash
RAILWAY_EXPECTED_COMMIT=e1fb4a8eee7fd5a4edf895b76cf0b4305880a34b npm run audit:railway
```

결과:

- `/`: HTTP 200, Next.js 루트 페이지 응답.
- `/api/version`: HTTP 200, `app=honzang`.
- `/api/health`: HTTP 200, `database=connected`.
- `/manifest.webmanifest`: HTTP 200, `display=standalone`.
- 배포 커밋: `e1fb4a8eee7fd5a4edf895b76cf0b4305880a34b` 일치.
- 보안 헤더: 루트와 `/api/version` 모두 기본 보안 헤더 응답.
- 요약: 0 fail, 0 warn, 6 pass.

```bash
RAILWAY_EXPECTED_COMMIT=e1fb4a8eee7fd5a4edf895b76cf0b4305880a34b npm run verify:railway
```

결과:

- `Railway verification passed at https://honzang-production.up.railway.app`

## 판단

- Railway 공개 도메인이 기존 static/legacy 페이지에서 최신 Next.js standalone 앱으로 정상 전환되었다.
- Railway Postgres 연결도 `/api/health` 기준 `connected`로 확인되었다.
- PWA manifest와 보안 헤더도 공개 URL에 반영되어 있다.
- 운영 DB를 변경하는 `verify:db-workflow`는 테스트 데이터를 생성/정리하는 검증이므로, 운영 DB에서 실행하려면 별도 승인 후 진행한다.
