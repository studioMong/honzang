# 개발진행로그 70

## 작업 시각

- 2026-06-17 23:08 KST

## 확인 대상

- 커밋: `ae89245` (`Add backup readiness coverage`)
- 공개 URL: `https://honzang-production.up.railway.app`
- 검증 명령: `RAILWAY_EXPECTED_COMMIT=ae89245 npm run verify:railway`

## 결과

- Railway 공개 URL 검증 실패.
- `/`는 HTTP 200이지만 legacy 정적 프로젝트 요약 페이지로 응답.
- `/api/version`은 HTTP 404.
- `/api/health`는 HTTP 404.
- `/manifest.webmanifest`는 HTTP 404.

## 판단

- 같은 커밋에서 로컬 `npm run verify:mvp`는 통과했다.
- 로컬 프로덕션 빌드에는 Next.js API 라우트와 PWA manifest가 생성된다.
- 공개 URL이 계속 legacy 정적 페이지를 반환하므로, 현재 도메인이 최신 Next.js 서비스가 아니라 이전 정적 서비스에 붙어 있을 가능성이 높다.

## 필요한 외부 확인

- Railway Dashboard에서 `honzang-production.up.railway.app` 도메인이 최신 GitHub `studioMong/honzang` `main` 브랜치 서비스에 연결되어 있는지 확인.
- 서비스 Root Directory가 저장소 루트인지 확인.
- 루트 `Dockerfile` 빌드가 사용되는지 확인.
- `DATABASE_URL`이 현재 Postgres 서비스 값으로 설정되어 있는지 확인.
- 배포 명령이 `npm run db:deploy` 후 `npm run start` 흐름인지 확인.
