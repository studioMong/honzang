# 개발 진행 로그 - 2026-06-18 16

## 목표
- CSV 템플릿 상태 표시 커밋 `be2d1cc0687354146eefa12cc5ca70ac83ba43b5` 기준으로 Railway 공개 주소 배포 상태를 재감사한다.

## 실행
- `RAILWAY_AUDIT_SOFT=1 RAILWAY_EXPECTED_COMMIT=be2d1cc0687354146eefa12cc5ca70ac83ba43b5 npm run audit:railway`

## 결과
- 감사 대상: `https://honzang-production.up.railway.app`
- `/`: HTTP 200, `text/html; charset=utf-8`, 24489 bytes
- `/api/version`: HTTP 404
- `/api/health`: HTTP 404
- `/manifest.webmanifest`: HTTP 404
- 요약: 4 fail, 0 warn, 0 pass

## 판단
- 공개 도메인은 아직 최신 Next.js 앱을 서빙하지 않는다.
- `/api/version`, `/api/health`, `/manifest.webmanifest`가 모두 404이므로 현재 도메인은 앱 서버 라우트가 노출된 서비스에 연결되어 있지 않다.
- 루트 페이지가 이전 프로젝트 요약 문구를 포함하는 legacy/static 응답으로 남아 있다.

## 다음 조치
- Railway Dashboard에서 public domain 연결 대상 서비스가 최신 GitHub `main` 배포 서비스인지 확인한다.
- Healthcheck Path를 `/api/health`로 설정한다.
- 도메인 연결 수정 뒤 `npm run audit:railway`와 `npm run smoke:prod`를 다시 실행한다.
