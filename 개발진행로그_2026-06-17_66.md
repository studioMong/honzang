# 개발진행로그 66

## 작업 시각

- 2026-06-17 23:12 KST

## 확인 대상

- 커밋: `9e4dc53` (`Add dashboard filing readiness summary`)
- 공개 URL: `https://honzang-production.up.railway.app`
- 검증 명령: `RAILWAY_EXPECTED_COMMIT=9e4dc53 npm run verify:railway`

## 결과

- Railway 공개 URL 검증 실패.
- `/`는 HTTP 200이지만 legacy 정적 프로젝트 요약 페이지로 응답.
- `/api/version`은 HTTP 404.
- `/api/health`는 HTTP 404.
- `/manifest.webmanifest`는 HTTP 404.

## 판단

- `npm run verify:mvp`는 로컬/저장소 기준으로 통과했다.
- 빌드 산출물에는 Next.js API 라우트와 PWA manifest가 포함되어 있다.
- 공개 URL만 legacy 정적 페이지를 반환하므로 Railway 도메인이 최신 Next.js standalone 서비스가 아닌 다른 서비스 또는 이전 정적 배포에 연결된 상태다.

## 필요한 외부 확인

- Railway Dashboard에서 도메인 `honzang-production.up.railway.app`이 현재 GitHub `studioMong/honzang` `main` 브랜치 서비스에 붙어 있는지 확인.
- 해당 서비스가 루트 `Dockerfile`로 빌드되는지 확인.
- Root Directory가 저장소 루트인지 확인.
- Start Command가 `npm run start`, Pre-deploy가 `npm run db:deploy`인지 확인.
- `DATABASE_URL`이 Railway Postgres 참조 변수로 연결되어 있는지 확인.
