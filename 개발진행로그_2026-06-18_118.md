# 개발 진행 로그 118

## 작업 단위
- 백업 내보내기 포함 상태 표시 커밋(`4d6f2fbd9e98fc03a1473a5e5bf03337c8acea50`) 이후 공개 Railway 배포 상태를 감사했다.

## 실행 명령
- `RAILWAY_AUDIT_SOFT=1 RAILWAY_EXPECTED_COMMIT=4d6f2fbd9e98fc03a1473a5e5bf03337c8acea50 npm run audit:railway`

## 감사 결과
- 공개 주소: `https://honzang-production.up.railway.app`
- 기대 커밋: `4d6f2fbd9e98fc03a1473a5e5bf03337c8acea50`
- `/`: HTTP 200, `text/html; charset=utf-8`, 24489 bytes
- `/api/version`: HTTP 404
- `/api/health`: HTTP 404
- `/manifest.webmanifest`: HTTP 404
- 요약: 5 fail, 0 warn, 0 pass

## 실패 항목
- `Next.js API 라우트 미노출`
- `Legacy 정적 페이지 응답`
- `Healthcheck 미노출`
- `PWA manifest 미노출`
- `보안 헤더 누락`

## 판단
- 최신 GitHub `main` 커밋은 푸시됐지만 공개 Railway 도메인은 여전히 최신 Next.js standalone 서비스가 아니다.
- `/api/version`, `/api/health`, `/manifest.webmanifest`가 모두 404이며, 코드에 포함된 보안 헤더도 공개 응답에 반영되지 않는다.

## 다음 조치
- Railway Dashboard에서 public domain 연결 대상 서비스를 최신 Next.js standalone 서비스로 전환한다.
- 전환 후 `RAILWAY_EXPECTED_COMMIT=$(git rev-parse HEAD) npm run verify:railway`를 실행한다.
