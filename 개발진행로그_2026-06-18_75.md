# 개발 진행 로그 2026-06-18 75

## 작업 범위
- 백업 복원 dry-run에서 JSON payload 계열 데이터의 크기를 검증하도록 보강했다.

## 구현 내용
- `src/app/api/backups/restore/route.ts`
  - `MAX_BACKUP_JSON_PAYLOAD_SIZE`를 500,000바이트로 정의했다.
  - `validateBackupJsonPayloads`를 추가했다.
  - 리포트 `calculatedPayload`, 마감 `summaryPayload`, 활동 로그 `metadata`가 기준 크기를 넘으면 `INVALID_BACKUP_JSON_PAYLOADS` 이슈로 반환한다.
  - 큰 JSON payload가 Postgres JSON 저장과 백업 복원 처리를 과도하게 흔드는 상황을 dry-run에서 미리 차단한다.

## 테스트 보강
- `scripts/verify-backup-restore.mjs`
  - 리포트 계산 결과, 마감 요약, 활동 로그 metadata에 500KB 초과 payload가 들어온 백업 JSON을 HTTP 400과 `INVALID_BACKUP_JSON_PAYLOADS`로 거절하는지 확인한다.

## 검증
- `npm run typecheck`
- `npm run lint`
- `git diff --check`
- `npm run verify:mvp`

## 결과
- 전체 MVP 검증이 통과했다.
- 공개 Railway 도메인의 legacy/static 연결 문제는 별도 감사 단계에서 계속 확인해야 한다.
