# 개발 진행 로그 2026-06-18 77

## 작업 범위
- 리포트 저장과 마감 저장 API에서도 JSON payload 크기를 검증하도록 보강했다.

## 구현 내용
- `src/lib/server/json-payload-validation.ts`
  - `MAX_JSON_PAYLOAD_SIZE` 500,000바이트 기준과 `validateJsonPayloadSize` 공통 헬퍼를 추가했다.
- `src/app/api/backups/restore/route.ts`
  - 백업 복원 JSON payload 크기 검증이 공통 헬퍼를 사용하도록 정리했다.
- `src/app/api/reports/route.ts`
  - 리포트 `calculatedPayload`가 기준 크기를 넘으면 `INVALID_REPORT_PAYLOAD`로 거절한다.
- `src/app/api/closing-periods/route.ts`
  - 마감 `summaryPayload`가 기준 크기를 넘으면 `INVALID_CLOSING_PAYLOAD`로 거절한다.

## 테스트 보강
- `scripts/smoke-production.mjs`
  - `/api/reports`가 oversized `calculatedPayload`를 HTTP 400과 `INVALID_REPORT_PAYLOAD`로 거절하는지 확인한다.
  - `/api/closing-periods`가 oversized `summaryPayload`를 HTTP 400과 `INVALID_CLOSING_PAYLOAD`로 거절하는지 확인한다.

## 검증
- `npm run typecheck`
- `npm run lint`
- `git diff --check`
- `npm run verify:mvp`

## 결과
- 전체 MVP 검증이 통과했다.
- 공개 Railway 도메인의 legacy/static 연결 문제는 별도 감사 단계에서 계속 확인해야 한다.
