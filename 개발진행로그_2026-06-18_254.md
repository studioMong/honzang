# 개발진행로그 2026-06-18 254

## Railway 배포 감사
- 대상 URL: `https://honzang-production.up.railway.app`
- 기대 커밋: `0d6a8946dd7145c5575c4825aadfe8cc0a7e69cf`
- 실행 명령: `RAILWAY_AUDIT_SOFT=1 RAILWAY_EXPECTED_COMMIT=0d6a8946dd7145c5575c4825aadfe8cc0a7e69cf npm run audit:railway`

## 결과
- 요약: 5 fail, 0 warn, 0 pass
- `/`: HTTP 200이지만 타이틀이 `혼자장부 프로젝트 정리`인 legacy/static 페이지 응답
- `/api/version`: HTTP 404
- `/api/health`: HTTP 404
- `/manifest.webmanifest`: HTTP 404
- 최신 Next.js 보안 헤더 미반영

## 판단
- 최신 부가세 일정 보강 커밋은 GitHub에 푸시됐지만, Railway 공개 도메인은 여전히 최신 Next.js 앱으로 연결되지 않았다.
- 로컬 `npm run verify:mvp`와 브라우저 검증은 통과했으므로, 코드보다 Railway 도메인 연결/서비스 선택 문제가 우선 차단점이다.

## 다음 조치
- Railway Dashboard에서 현재 public domain이 legacy/static 서비스가 아니라 Dockerfile 기반 Next.js 서비스에 연결되어 있는지 확인한다.
- 도메인 전환 후 `/api/version`, `/api/health`, `/manifest.webmanifest`가 200 응답하는지 확인한다.
- 전환 완료 뒤 `RAILWAY_EXPECTED_COMMIT=$(git rev-parse HEAD) npm run verify:railway`를 실행한다.
