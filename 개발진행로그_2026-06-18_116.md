# 개발 진행 로그 116

## 작업 단위
- 거래 수정 UI 실패 롤백 커밋(`a9e3314966e679b68ebc86df694c05f1873a745b`) 이후 공개 Railway 배포 상태를 감사했다.

## 실행 명령
- `RAILWAY_AUDIT_SOFT=1 RAILWAY_EXPECTED_COMMIT=a9e3314966e679b68ebc86df694c05f1873a745b npm run audit:railway`

## 감사 결과
- 공개 주소: `https://honzang-production.up.railway.app`
- 기대 커밋: `a9e3314966e679b68ebc86df694c05f1873a745b`
- `/`: HTTP 200, `text/html; charset=utf-8`, 24489 bytes
- `/api/version`: HTTP 404
- `/api/health`: HTTP 404
- `/manifest.webmanifest`: HTTP 404
- 요약: 5 fail, 0 warn, 0 pass

## 실패 항목
- `Next.js API 라우트 미노출`
- `Legacy 정적 페이지 응답`
- `Healthcheck 미노출`
- `PWA manifest 미노출`
- `보안 헤더 누락`

## 판단
- GitHub `main` 최신 커밋은 푸시됐지만 공개 Railway 도메인은 여전히 최신 Next.js 앱을 바라보지 않는다.
- 코드의 `/api/version`, `/api/health`, PWA manifest, 보안 헤더가 공개 URL에 노출되지 않는 상태다.

## 다음 조치
- Railway Dashboard에서 public domain 연결 대상과 Healthcheck Path를 최신 Next.js standalone 서비스로 전환한다.
- 전환 후 `RAILWAY_EXPECTED_COMMIT=$(git rev-parse HEAD) npm run verify:railway`를 실행한다.
