# 개발 진행 로그 - 2026-06-18 25

## 목표
- CSV 매핑 템플릿이 전체 백업/복원 dry-run 검증에도 포함되게 한다.
- 헤더 구조가 다른 템플릿 여러 개가 백업 데이터로 계산되는지 확인한다.

## 변경 내용
- `scripts/verify-backup-restore.mjs`
  - 백업 fixture의 `csvTemplates`에 BANK 템플릿 2개를 추가했다.
  - 설정 화면 검증에 `CSV 매핑 템플릿` 섹션 확인을 추가했다.
  - dry-run 복원 카운트에서 `csvTemplates`가 2개로 계산되는지 확인한다.

## 검증
- `npm run lint` 통과.
- `git diff --check` 통과.
- `npm run verify:backup-restore` 통과.
- `npm run verify:mvp` 통과.

## 판단
- CSV 템플릿 저장/삭제/다중 헤더 보존 흐름이 백업 dry-run 검증에도 연결됐다.
- 운영자가 내려받은 백업에 CSV 매핑 템플릿이 누락되거나 카운트되지 않는 회귀를 더 빨리 잡을 수 있다.
