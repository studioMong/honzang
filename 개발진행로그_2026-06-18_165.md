# 개발 진행 로그 165

## 작업 단위
- 운영 배포에 필요한 접근 보호 환경변수 예시를 저장소에 포함했다.
- 코드 커밋(`6c73ec511f4432d1ccfc996f45c1f5915e2f019f`) 이후 공개 Railway 배포 상태를 다시 감사했다.

## 구현 내용
- `.env.example`을 Git 추적 대상으로 만들기 위해 `.gitignore`에 예외를 추가했다.
- `.env.example`에 `HONZANG_ACCESS_CODE`, `HONZANG_ACCESS_TOKEN_SALT` 예시를 추가했다.
- `scripts/verify-deployment-config.mjs`가 `.env.example`의 접근 보호 변수 존재를 검증하게 했다.

## 검증
- `npm run typecheck`: 통과
- `npm run lint`: 통과
- `git diff --check`: 통과
- `npm run verify:mvp`: 통과
- `DATABASE_URL`: 현재 셸에 없어 `npm run verify:db-workflow`는 실행하지 못했다.

## Railway 감사
- 실행 명령: `RAILWAY_AUDIT_SOFT=1 RAILWAY_EXPECTED_COMMIT=6c73ec511f4432d1ccfc996f45c1f5915e2f019f npm run audit:railway`
- 공개 주소: `https://honzang-production.up.railway.app`
- 기대 커밋: `6c73ec511f4432d1ccfc996f45c1f5915e2f019f`
- `/`: HTTP 200, `text/html; charset=utf-8`, 24489 bytes, `server=railway-hikari`, `x-railway-request-id=e5qDHT1mR32vUuDeVOLIQQ`
- 루트 미리보기: `<title>혼자장부 프로젝트 정리</title>`
- `/api/version`: HTTP 404, `server=railway-hikari`, `x-railway-request-id=nhvGo_aUT2au2mBgoB_USg`
- `/api/health`: HTTP 404, `server=railway-hikari`, `x-railway-request-id=1lJPIWJaSQiEmBN8-8Y8hA`
- `/manifest.webmanifest`: HTTP 404, `server=railway-hikari`, `x-railway-request-id=jYvTWczeTpubbS1aacI7Nw`
- 요약: 5 fail, 0 warn, 0 pass

## 판단
- 최신 커밋은 GitHub `main`에 푸시됐지만 공개 Railway 도메인은 여전히 이전 정적 페이지 서비스에 연결되어 있다.
- 코드와 로컬 standalone 빌드는 검증을 통과했으므로 남은 공개 배포 이슈는 Railway Dashboard의 public domain 연결 대상 문제로 본다.

## 다음 조치
- Railway Dashboard에서 `honzang-production.up.railway.app` public domain이 붙은 서비스를 최신 Next.js standalone 앱 서비스로 전환한다.
- 앱 서비스 Variables에 `DATABASE_URL`, `NEXT_PUBLIC_APP_URL`, `HONZANG_ACCESS_CODE`, `HONZANG_ACCESS_TOKEN_SALT`를 설정한다.
- 전환 후 `RAILWAY_EXPECTED_COMMIT=$(git rev-parse HEAD) npm run verify:railway`를 실행한다.
