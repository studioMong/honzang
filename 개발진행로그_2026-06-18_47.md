# 개발 진행 로그 2026-06-18 47

## 작업 범위
- 백업 복원 API의 분개 무결성 검증을 추가했다.
- 복원 전 dry-run에서 누락 거래 참조, 누락 계정과목, 불균형 분개, 잘못된 차변/대변 라인을 거절하도록 했다.
- 백업 복원 검증 스크립트에 깨진 분개 백업 거절 케이스를 추가했다.

## 변경 파일
- `src/app/api/backups/restore/route.ts`
- `scripts/verify-backup-restore.mjs`

## 검증
- `npm run typecheck` 통과
- `npm run lint` 통과
- `git diff --check` 통과
- `npm run verify:mvp` 통과

## 메모
- 백업 복원은 더 이상 계정과목 없는 분개 라인을 조용히 건너뛰지 않는다.
- 원장 품질에 직접 영향을 주는 분개 데이터는 복원 전 명시적으로 실패시키고 issues로 반환한다.
