# 개발 진행 로그 100

## 작업 단위
- production 보안 헤더 커밋(`d69b07c47bfd7efd750918bd517f4133f173b5e9`) 이후 공개 Railway 배포 상태를 감사했다.

## 실행 명령
- `RAILWAY_AUDIT_SOFT=1 RAILWAY_EXPECTED_COMMIT=d69b07c47bfd7efd750918bd517f4133f173b5e9 npm run audit:railway`

## 감사 결과
- 공개 주소: `https://honzang-production.up.railway.app`
- 기대 커밋: `d69b07c47bfd7efd750918bd517f4133f173b5e9`
- `/`: HTTP 200, `text/html; charset=utf-8`, 24489 bytes
- `/api/version`: HTTP 404
- `/api/health`: HTTP 404
- `/manifest.webmanifest`: HTTP 404
- 요약: 4 fail, 0 warn, 0 pass

## 판단
- 공개 도메인은 아직 최신 Next.js standalone 서비스가 아니라 기존 legacy/static 서비스에 연결된 상태다.
- 보안 헤더 커밋은 GitHub `main`에 반영됐지만 공개 URL에는 아직 반영되지 않았다.

## 다음 조치
- Railway Dashboard에서 public domain 연결 대상 서비스를 최신 Next.js 앱으로 전환한다.
- 전환 후 `/`, `/api/version`, `/api/health`, `/manifest.webmanifest`와 보안 헤더 응답을 확인한다.
- `RAILWAY_EXPECTED_COMMIT=$(git rev-parse HEAD) npm run verify:railway`를 실행한다.
