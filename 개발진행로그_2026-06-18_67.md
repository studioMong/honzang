# 개발 진행 로그 2026-06-18 67

## 작업 범위
- 백업 복원 dry-run에서 검토 항목의 연결 거래 참조가 유효한지 검증하도록 보강했다.

## 구현 내용
- `src/app/api/backups/restore/route.ts`
  - `validateBackupReviewItems`를 추가했다.
  - 검토 항목에 연결 거래가 없으면 `INVALID_BACKUP_REVIEW_ITEMS` 이슈로 반환한다.
  - 검토 항목의 연결 거래 ID가 백업 거래 목록에 없으면 `INVALID_BACKUP_REVIEW_ITEMS` 이슈로 반환한다.
  - 실제 복원 단계에서 검토 항목이 조용히 스킵되는 상황을 dry-run에서 미리 차단한다.

## 테스트 보강
- `scripts/verify-backup-restore.mjs`
  - 검토 항목이 존재하지 않는 거래 ID를 참조하는 백업 JSON을 HTTP 400과 `INVALID_BACKUP_REVIEW_ITEMS`로 거절하는지 확인한다.

## 검증
- `npm run typecheck`
- `npm run lint`
- `git diff --check`
- `npm run verify:mvp`

## 결과
- 전체 MVP 검증이 통과했다.
- 공개 Railway 도메인의 legacy/static 연결 문제는 별도 감사 단계에서 계속 확인해야 한다.
