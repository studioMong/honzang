# 개발 진행 로그 2026-06-18 50

## 작업 범위
- 리포트 기간 검증 커밋 이후 Railway 공개 도메인 상태를 다시 감사했다.

## 감사 명령
- `RAILWAY_AUDIT_SOFT=1 RAILWAY_EXPECTED_COMMIT=25df92550798488535b6318d930ce2a99ab9fc16 npm run audit:railway`

## 감사 결과
- 대상 URL: `https://honzang-production.up.railway.app`
- 기대 커밋: `25df92550798488535b6318d930ce2a99ab9fc16`
- `/`: HTTP 200, `text/html; charset=utf-8`, 24489 bytes
- `/api/version`: HTTP 404
- `/api/health`: HTTP 404
- `/manifest.webmanifest`: HTTP 404
- 요약: 4 fail, 0 warn, 0 pass

## 판단
- 공개 도메인은 아직 현재 Next.js standalone 서비스가 아니라 legacy/static 서비스에 연결된 상태로 보인다.
- 최신 커밋의 API 라우트와 PWA manifest가 공개 URL에 노출되지 않는다.

## Railway에서 필요한 조치
- `honzang-production.up.railway.app` public domain을 최신 Next.js 서비스에 연결한다.
- 기존 legacy/static 서비스가 도메인을 점유 중이면 연결을 해제한다.
- 새 서비스의 healthcheck path를 `/api/health`로 지정한다.
