# 개발진행로그 2026-06-18 4

## Railway 공개 URL 재점검
- 기준 커밋: `71edf41`
- 점검 명령: `RAILWAY_EXPECTED_COMMIT=71edf41 npm run audit:railway`
- 대상 URL: `https://honzang-production.up.railway.app`

## 결과
- `/`: HTTP 200, 레거시 정적 HTML 응답.
- `/api/version`: HTTP 404.
- `/api/health`: HTTP 404.
- `/manifest.webmanifest`: HTTP 404.
- 요약: `4 fail, 0 warn, 0 pass`.

## 판단
- 원격 저장소에는 최신 코드가 푸시되어 있지만 공개 Railway 도메인은 아직 Next.js 앱 서비스가 아니라 이전 정적 서비스에 연결된 상태다.
- 코드 변경이나 로컬 standalone 빌드 문제가 아니라 Railway 서비스/도메인/배포 소스 연결 문제로 봐야 한다.

## 필요한 확인
- Railway Dashboard에서 `honzang-production.up.railway.app` 도메인이 연결된 서비스가 현재 저장소의 Next.js 서비스인지 확인.
- 최신 배포 커밋이 `71edf41` 이상인지 확인.
- Dockerfile 빌드, `/api/health` healthcheck, `DATABASE_URL`, `HONZANG_ACCESS_CODE`, `NEXT_PUBLIC_APP_URL`이 같은 서비스에 설정되어 있는지 확인.
