# 개발진행로그 49 - 월 마감 잠금 푸시 및 Railway 확인

## 원격 반영

- 커밋: `0c86a24 Add monthly closing period locks`
- 원격: `git@github.com:studioMong/honzang.git`
- 브랜치: `main`
- 푸시 결과: 성공

## Railway 확인

- 실행: `RAILWAY_EXPECTED_COMMIT=0c86a24f164af1dbf7ac74066832e8bb53e96b32 npm run verify:railway`
- 결과: 실패
- 로그 커밋 푸시 후 최신 HEAD 기준 `RAILWAY_EXPECTED_COMMIT=4be6989 npm run verify:railway`도 동일하게 실패
- `/api/version`: HTTP 404
- `/api/health`: HTTP 404
- `/manifest.webmanifest`: HTTP 404
- `/`: HTTP 200이지만 기존 정적 프로젝트 요약 페이지로 감지됨

## 판단

- GitHub 원격에는 최신 커밋이 올라갔다.
- 현재 공개 도메인 `https://honzang-production.up.railway.app`은 아직 최신 Next.js 서비스가 아니라 기존 정적 사이트 또는 다른 서비스에 연결된 상태로 보인다.
- Railway 쪽 도메인 연결, 서비스 연결, Dockerfile 빌드 감지, `DATABASE_URL`/배포 트리거 상태 확인이 필요하다.
