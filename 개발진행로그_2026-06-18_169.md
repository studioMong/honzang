# 개발 진행 로그 169

## 작업 단위
- 운영 접근 보호의 로그인 요청 검증을 강화했다.
- 코드 커밋(`84b2dd175dbbba326c83598f0f9402e62a7fcc24`) 이후 공개 Railway 배포 상태를 다시 감사했다.

## 구현 내용
- `src/app/api/auth/login/route.ts`에 로그인 요청 본문 최대 크기(`2,048` bytes)를 추가했다.
- `code` 필드를 `zod` 스키마로 검증해 빈 값, 비문자열, 200자 초과 값을 거부하게 했다.
- 잘못된 JSON은 `INVALID_LOGIN_PAYLOAD`와 HTTP 400으로 응답하게 했다.
- 과대 로그인 본문은 `LOGIN_PAYLOAD_TOO_LARGE`와 HTTP 413으로 응답하게 했다.
- `scripts/verify-access-control.mjs`에 invalid JSON 로그인 요청과 과대 로그인 요청 검증을 추가했다.

## 검증
- `npm run typecheck`: 통과
- `npm run lint`: 통과
- `git diff --check`: 통과
- `npm run verify:mvp`: 통과
- `DATABASE_URL`: 현재 셸에 없어 `npm run verify:db-workflow`는 실행하지 못했다.

## Railway 감사
- 실행 명령: `RAILWAY_AUDIT_SOFT=1 RAILWAY_EXPECTED_COMMIT=84b2dd175dbbba326c83598f0f9402e62a7fcc24 npm run audit:railway`
- 공개 주소: `https://honzang-production.up.railway.app`
- 기대 커밋: `84b2dd175dbbba326c83598f0f9402e62a7fcc24`
- `/`: HTTP 200, `text/html; charset=utf-8`, 24489 bytes, `server=railway-hikari`, `x-railway-request-id=CtaQUKo4S_eGowab-8Y8hA`
- 루트 미리보기: `<title>혼자장부 프로젝트 정리</title>`
- `/api/version`: HTTP 404, `server=railway-hikari`, `x-railway-request-id=EOIcZRwwQwaQsnX1-8Y8hA`
- `/api/health`: HTTP 404, `server=railway-hikari`, `x-railway-request-id=dxC2ugGlQC-8rtMmacI7Nw`
- `/manifest.webmanifest`: HTTP 404, `server=railway-hikari`, `x-railway-request-id=HMe8ROQ5SSOMbH29-8Y8hA`
- 요약: 5 fail, 0 warn, 0 pass

## 판단
- 최신 커밋은 GitHub `main`에 푸시됐지만 공개 Railway 도메인은 여전히 이전 정적 페이지 서비스에 연결되어 있다.
- 로그인 요청 검증, access-control 회귀 검증, 로컬 standalone 빌드, MVP 검증은 완료됐다.
- 남은 공개 배포 이슈는 Railway Dashboard의 public domain 연결 대상 문제다.

## 다음 조치
- Railway Dashboard에서 `honzang-production.up.railway.app` public domain을 최신 Next.js standalone 앱 서비스로 전환한다.
- 전환 후 `RAILWAY_EXPECTED_COMMIT=$(git rev-parse HEAD) npm run verify:railway`를 실행한다.
