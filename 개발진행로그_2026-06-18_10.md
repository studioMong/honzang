# 개발진행로그 10 - 통장 잔액 대조 커밋 후 Railway 재점검

## 기준 커밋

- 커밋: `2d13b05e78ed51f2263eb3d9b2adbd018427844e`
- 원격: `origin/main`

## Railway 공개 URL 재점검

- 대상: `https://honzang-production.up.railway.app`
- 명령: `RAILWAY_AUDIT_SOFT=1 RAILWAY_EXPECTED_COMMIT=2d13b05e78ed51f2263eb3d9b2adbd018427844e npm run audit:railway`

## 결과

- `/`: HTTP 200, legacy/static HTML 응답
- `/api/version`: HTTP 404
- `/api/health`: HTTP 404
- `/manifest.webmanifest`: HTTP 404
- Summary: `4 fail, 0 warn, 0 pass`

## 판단

- 최신 코드의 Next.js API 라우트와 PWA manifest가 공개 URL에 노출되지 않는다.
- 통장 잔액 대조 기능 커밋은 GitHub `origin/main`에 푸시됐지만, Railway 공개 도메인은 아직 현재 Next.js standalone 서비스가 아니라 이전 정적 서비스에 연결된 상태다.

## Railway Dashboard 확인 필요

- `honzang-production.up.railway.app` 도메인이 붙은 서비스가 현재 GitHub `studioMong/honzang` `main` 브랜치 서비스인지 확인한다.
- Deploy source commit이 `2d13b05e78ed51f2263eb3d9b2adbd018427844e`인지 확인한다.
- Root Directory가 저장소 루트인지 확인한다.
- 빌더가 루트 `Dockerfile`을 사용하는지 확인한다.
- Start Command가 `npm run start`인지 확인한다.
- Pre-deploy Command가 `npm run db:deploy`인지 확인한다.
- Healthcheck Path가 `/api/health`인지 확인한다.
- `DATABASE_URL`, `HONZANG_ACCESS_CODE`, `HONZANG_ACCESS_TOKEN_SALT`, `NEXT_PUBLIC_APP_URL` 변수가 앱 서비스에 연결돼 있는지 확인한다.
