# 개발 진행 로그 - 2026-06-18 14

## 목표
- 마감 차단 변경 커밋 `26403c6e9d5d595d46e566ad548a15dde1590452` 기준으로 Railway 공개 주소 배포 상태를 재감사한다.

## 실행
- `RAILWAY_AUDIT_SOFT=1 RAILWAY_EXPECTED_COMMIT=26403c6e9d5d595d46e566ad548a15dde1590452 npm run audit:railway`

## 결과
- 감사 대상: `https://honzang-production.up.railway.app`
- `/`: HTTP 200, `text/html; charset=utf-8`, 24489 bytes
- `/api/version`: HTTP 404
- `/api/health`: HTTP 404
- `/manifest.webmanifest`: HTTP 404
- 요약: 4 fail, 0 warn, 0 pass

## 판단
- 공개 도메인은 아직 최신 Next.js 앱을 서빙하지 않는다.
- `/api/version`, `/api/health`, `/manifest.webmanifest`가 모두 노출되지 않아 Railway 도메인이 현재 앱 서비스가 아니라 기존 legacy/static 서비스에 연결된 상태로 보인다.
- 루트 페이지에도 이전 프로젝트 요약 문구가 남아 있어 배포 대상 서비스 또는 public domain 연결을 Railway 대시보드에서 수정해야 한다.

## 다음 조치
- Railway에서 `honzang-production.up.railway.app` public domain이 최신 Next.js 서비스에 연결되어 있는지 확인한다.
- Healthcheck Path는 `/api/health`로 설정한다.
- 도메인 연결 수정 후 `npm run audit:railway`와 `npm run smoke:production`을 다시 실행한다.
