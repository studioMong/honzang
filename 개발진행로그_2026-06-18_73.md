# 개발 진행 로그 2026-06-18 73

## 작업 범위
- 백업 복원 dry-run에서 원본 CSV 파일의 가져오기 배치 참조가 유효한지 검증하도록 보강했다.

## 구현 내용
- `src/app/api/backups/restore/route.ts`
  - `validateBackupOriginalImportFiles`가 전체 백업 객체를 받아 가져오기 배치 목록과 원본 CSV 목록을 함께 확인하도록 변경했다.
  - 원본 CSV `importBatchId`가 백업 가져오기 목록에 없으면 `INVALID_BACKUP_ORIGINAL_FILES` 이슈로 반환한다.
  - 기존 원본 CSV 텍스트/파일 크기 검증은 유지했다.
  - dry-run 카운트에는 잡히지만 실제 복원에서 연결되지 않는 원본 CSV를 미리 차단한다.

## 테스트 보강
- `scripts/verify-backup-restore.mjs`
  - 원본 CSV 파일 크기 불일치와 누락된 가져오기 배치 참조를 함께 검증한다.

## 검증
- `npm run typecheck`
- `npm run lint`
- `git diff --check`
- `npm run verify:mvp`

## 결과
- 전체 MVP 검증이 통과했다.
- 공개 Railway 도메인의 legacy/static 연결 문제는 별도 감사 단계에서 계속 확인해야 한다.
