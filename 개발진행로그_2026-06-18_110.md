# 개발 진행 로그 110

## 작업 단위
- 승인 분개 덮어쓰기 차단 커밋(`d2f500cdc417907e855e0b04636e89903d716ea2`) 이후 공개 Railway 배포 상태를 감사했다.

## 실행 명령
- `RAILWAY_AUDIT_SOFT=1 RAILWAY_EXPECTED_COMMIT=d2f500cdc417907e855e0b04636e89903d716ea2 npm run audit:railway`

## 감사 결과
- 공개 주소: `https://honzang-production.up.railway.app`
- 기대 커밋: `d2f500cdc417907e855e0b04636e89903d716ea2`
- `/`: HTTP 200, `text/html; charset=utf-8`, 24489 bytes
- `/api/version`: HTTP 404
- `/api/health`: HTTP 404
- `/manifest.webmanifest`: HTTP 404
- 요약: 5 fail, 0 warn, 0 pass

## 실패 항목
- `Next.js API 라우트 미노출`
- `Legacy 정적 페이지 응답`
- `Healthcheck 미노출`
- `PWA manifest 미노출`
- `보안 헤더 누락`

## 판단
- GitHub `main` 최신 커밋은 푸시됐지만 공개 Railway 도메인은 여전히 legacy/static 서비스에 연결되어 있다.
- 루트 응답 크기, API 404, manifest 404 상태가 이전 감사와 동일하다.
- 현재 실패는 코드 변경 사항이 아닌 Railway public domain/서비스 연결 문제로 판단한다.

## 다음 조치
- Railway Dashboard에서 public domain을 최신 Next.js standalone 서비스로 전환한다.
- 전환 후 `RAILWAY_EXPECTED_COMMIT=$(git rev-parse HEAD) npm run verify:railway`를 실행한다.
