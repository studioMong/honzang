# 개발 진행 로그 98

## 작업 단위
- 프로덕션 접근 쿠키 salt 필수화 커밋(`33693a54addec81a394711c4975519f7194aa4e0`) 이후 공개 Railway 배포 상태를 감사했다.

## 실행 명령
- `RAILWAY_AUDIT_SOFT=1 RAILWAY_EXPECTED_COMMIT=33693a54addec81a394711c4975519f7194aa4e0 npm run audit:railway`

## 감사 결과
- 공개 주소: `https://honzang-production.up.railway.app`
- 기대 커밋: `33693a54addec81a394711c4975519f7194aa4e0`
- `/`: HTTP 200, `text/html; charset=utf-8`, 24489 bytes
- `/api/version`: HTTP 404
- `/api/health`: HTTP 404
- `/manifest.webmanifest`: HTTP 404
- 요약: 4 fail, 0 warn, 0 pass

## 판단
- 공개 도메인은 아직 최신 Next.js standalone 서비스가 아니라 기존 legacy/static 서비스에 연결된 상태다.
- 접근제어 강화 커밋은 GitHub `main`에 반영됐지만 공개 URL에는 아직 반영되지 않았다.

## 다음 조치
- Railway Dashboard에서 public domain 연결 대상 서비스를 최신 Next.js 앱으로 전환한다.
- 전환 후 `HONZANG_ACCESS_CODE`와 `HONZANG_ACCESS_TOKEN_SALT`가 모두 설정되어 있는지 운영 준비 점검에서 확인한다.
- `RAILWAY_EXPECTED_COMMIT=$(git rev-parse HEAD) npm run verify:railway`를 실행한다.
