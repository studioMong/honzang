# 개발 진행 로그 2026-06-18 71

## 작업 범위
- 백업 복원 dry-run에서 CSV 매핑 템플릿의 필수 매핑과 헤더 참조를 검증하도록 보강했다.

## 구현 내용
- `src/app/api/backups/restore/route.ts`
  - `validateBackupCsvTemplates`를 추가했다.
  - 거래일, 내용/적요, 금액 또는 입금/출금 매핑이 없으면 `INVALID_BACKUP_CSV_TEMPLATES` 이슈로 반환한다.
  - 알려진 매핑 필드 값이 문자열이 아니면 `INVALID_BACKUP_CSV_TEMPLATES` 이슈로 반환한다.
  - `headerSignature`가 있는 경우 매핑 컬럼이 헤더 서명 안에 없으면 `INVALID_BACKUP_CSV_TEMPLATES` 이슈로 반환한다.
  - 깨진 백업 템플릿이 복원 후 CSV 가져오기 화면과 자동 매핑을 흔드는 상황을 dry-run에서 미리 차단한다.

## 테스트 보강
- `scripts/verify-backup-restore.mjs`
  - 필수 매핑 누락, 비문자열 매핑 값, 헤더 서명에 없는 컬럼 참조를 포함한 백업 JSON을 HTTP 400과 `INVALID_BACKUP_CSV_TEMPLATES`로 거절하는지 확인한다.

## 검증
- `npm run typecheck`
- `npm run lint`
- `git diff --check`
- `npm run verify:mvp`

## 결과
- 전체 MVP 검증이 통과했다.
- 공개 Railway 도메인의 legacy/static 연결 문제는 별도 감사 단계에서 계속 확인해야 한다.
