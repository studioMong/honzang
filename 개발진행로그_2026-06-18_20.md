# 개발 진행 로그 - 2026-06-18 20

## 목표
- CSV 템플릿 삭제 기능 커밋 `ee5eb1f749ef579ad7057fec4c2d9d8560cd2fbb` 기준으로 Railway 공개 주소 배포 상태를 재감사한다.

## 실행
- `RAILWAY_AUDIT_SOFT=1 RAILWAY_EXPECTED_COMMIT=ee5eb1f749ef579ad7057fec4c2d9d8560cd2fbb npm run audit:railway`

## 결과
- 감사 대상: `https://honzang-production.up.railway.app`
- `/`: HTTP 200, `text/html; charset=utf-8`, 24489 bytes
- `/api/version`: HTTP 404
- `/api/health`: HTTP 404
- `/manifest.webmanifest`: HTTP 404
- 요약: 4 fail, 0 warn, 0 pass

## 판단
- 공개 도메인은 아직 최신 Next.js 앱을 서빙하지 않는다.
- `/api/version`, `/api/health`, `/manifest.webmanifest`가 404이므로 앱 서버 라우트가 공개 URL에 노출되지 않는다.
- 루트 응답도 이전 프로젝트 요약 문구가 있는 legacy/static 페이지로 남아 있다.

## 다음 조치
- Railway Dashboard에서 `honzang-production.up.railway.app` public domain이 최신 Next.js 서비스에 연결되어 있는지 확인한다.
- 서비스의 Deploy source commit이 GitHub `main` 최신 커밋과 일치하는지 확인한다.
- 도메인 연결 수정 후 `npm run audit:railway`와 `npm run smoke:prod`를 다시 실행한다.
