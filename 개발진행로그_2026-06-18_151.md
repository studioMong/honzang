# 개발 진행 로그 151

## 작업 단위
- 프로덕션 smoke 검증에 public 샘플 CSV URL 확인을 추가했다.
- 통장, 카드, 홈택스 매출, 홈택스 매입, 현금영수증, PG 정산 샘플이 standalone 서버에서 내려오는지 확인하도록 했다.

## 변경 파일
- `scripts/smoke-production.mjs`

## 검증
- `npm run typecheck`: 통과
- `npm run lint`: 통과
- `git diff --check`: 통과
- `npm run verify:mvp`: 통과

## DB 검증 상태
- 현재 셸에 `DATABASE_URL`이 없어 `npm run verify:db-workflow`는 실행하지 못했다.

## 비고
- `npm run verify:mvp`의 production smoke 단계에서 샘플 CSV URL 검증이 함께 수행됐다.
