# 개발 진행 로그 92

## 작업 단위
- Railway 감사 스크립트 보강 커밋(`574465b5e4e943cd9d7731f5b236e66f78f2aa1a`) 이후 공개 배포 상태를 다시 감사했다.

## 실행 명령
- `RAILWAY_AUDIT_SOFT=1 RAILWAY_EXPECTED_COMMIT=574465b5e4e943cd9d7731f5b236e66f78f2aa1a npm run audit:railway`

## 감사 결과
- 공개 주소: `https://honzang-production.up.railway.app`
- 기대 커밋: `574465b5e4e943cd9d7731f5b236e66f78f2aa1a`
- `/`: HTTP 200, `text/html; charset=utf-8`, 24489 bytes
- `/api/version`: HTTP 404
- `/api/health`: HTTP 404
- `/manifest.webmanifest`: HTTP 404
- 요약: 4 fail, 0 warn, 0 pass

## 확인된 출력 개선
- `Next.js API 라우트 미노출`, `Healthcheck 미노출` 항목이 Railway Dashboard의 `Public Networking`, `Deployments`, `Variables`, `Observability` 확인을 안내한다.
- 실패 항목과 보고서 마지막에 `docs/railway-cutover.md` 전환 체크리스트가 출력된다.

## 판단
- 공개 도메인은 아직 최신 Next.js standalone 서비스가 아니라 기존 legacy/static 서비스에 연결된 상태다.
- 코드와 배포 설정은 검증을 통과했으므로 남은 조치는 Railway Dashboard에서 public domain 연결 대상과 서비스 배포 구성을 전환하는 것이다.

## 다음 조치
- Railway Dashboard에서 `honzang-production.up.railway.app` public domain을 최신 Next.js 앱 서비스에 연결한다.
- `DATABASE_URL`, `HONZANG_ACCESS_CODE`, `HONZANG_ACCESS_TOKEN_SALT`, `NEXT_PUBLIC_APP_URL` 변수를 확인한다.
- 전환 후 `RAILWAY_EXPECTED_COMMIT=$(git rev-parse HEAD) npm run verify:railway`를 실행한다.
