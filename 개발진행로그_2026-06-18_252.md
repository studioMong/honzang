# 개발진행로그 2026-06-18 252

## Railway 배포 감사
- 대상 URL: `https://honzang-production.up.railway.app`
- 기대 커밋: `e81c291a94052d9f42bfacc002ffae2f97301836`
- 실행 명령: `RAILWAY_AUDIT_SOFT=1 RAILWAY_EXPECTED_COMMIT=e81c291a94052d9f42bfacc002ffae2f97301836 npm run audit:railway`

## 결과
- 요약: 5 fail, 0 warn, 0 pass
- `/`: HTTP 200이지만 타이틀이 `혼자장부 프로젝트 정리`인 legacy/static 페이지 응답
- `/api/version`: HTTP 404
- `/api/health`: HTTP 404
- `/manifest.webmanifest`: HTTP 404
- 최신 Next.js 보안 헤더 미반영

## 판단
- 원격 저장소의 최신 코드 커밋 `e81c291a94052d9f42bfacc002ffae2f97301836`은 푸시됐지만, Railway 공개 도메인은 최신 Next.js 앱을 바라보지 않는다.
- 로컬 MVP 검증과 브라우저 검증은 통과했으므로, 현재 운영 차단점은 코드 빌드보다는 Railway 도메인/서비스 연결 상태다.

## 다음 조치
- Railway Dashboard에서 `honzang-production.up.railway.app` Public Networking 대상 서비스를 최신 Next.js 앱으로 전환한다.
- Healthcheck Path를 `/api/health`로 설정하고, `DATABASE_URL`, `NEXT_PUBLIC_APP_URL`, 접근코드, 토큰 salt, 파일 암호화 키 변수를 확인한다.
- 전환 후 `npm run audit:railway` 또는 `RAILWAY_EXPECTED_COMMIT=$(git rev-parse HEAD) npm run verify:railway`를 다시 실행한다.
