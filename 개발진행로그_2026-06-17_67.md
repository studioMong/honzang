# 개발진행로그 67

## 작업 시각

- 2026-06-17 23:18 KST

## 목표

- 최근 추가한 대시보드 신고 준비율과 리포트 최종 신고 점검이 수동 브라우저 확인에만 의존하지 않도록 `verify:mvp` 자동 검증 범위에 포함한다.

## 구현 내용

- `scripts/smoke-production.mjs`의 루트 페이지 검사 강화.
- `/` 응답에서 아래 대시보드 핵심 UI 문구를 확인:
  - `혼자장부`
  - `최근 월 신고 준비`
  - `오늘 할 일`
  - `1인법인 신고 준비`
- `/?view=reports` 응답에서 아래 리포트 핵심 UI 문구를 확인:
  - `혼자장부 신고 준비 리포트`
  - `최종 신고 점검`
  - `자료 수집 현황`
  - `신고 패키지`
  - `재무제표 초안`
- README의 `verify:mvp` 설명에 production smoke가 대시보드와 리포트 핵심 UI까지 확인한다고 반영.

## 검증

- `npm run typecheck` 통과.
- `npm run lint` 통과.
- `npm run verify:mvp` 통과.

## 남은 확인

- Railway 공개 URL은 아직 최신 Next.js service가 아니라 legacy 정적 페이지를 반환하는 상태다.
- 저장소 내부 빌드와 smoke 범위는 강화됐으므로, 남은 배포 완료 조건은 Railway Dashboard의 서비스/도메인 연결 수정 후 `RAILWAY_EXPECTED_COMMIT=$(git rev-parse HEAD) npm run verify:railway` 통과다.
