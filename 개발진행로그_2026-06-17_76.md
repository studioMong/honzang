# 개발진행로그 76

## 작업 시각

- 2026-06-17 23:32 KST

## 확인 대상

- 커밋: `636cb0f` (`Add evidence deletion and data retention policy`)
- 공개 URL: `https://honzang-production.up.railway.app`
- 검증 명령:
  - `RAILWAY_EXPECTED_COMMIT=636cb0f npm run verify:railway`
  - `RAILWAY_AUDIT_SOFT=1 RAILWAY_EXPECTED_COMMIT=636cb0f npm run audit:railway`

## 결과

- Railway 공개 URL 검증 실패.
- `/`는 HTTP 200이지만 legacy 정적 프로젝트 요약 페이지로 응답.
- `/api/version`은 HTTP 404.
- `/api/health`은 HTTP 404.
- `/manifest.webmanifest`은 HTTP 404.

## 감사 스크립트 분류

- 결과 요약: `4 fail, 0 warn, 0 pass`
- 실패 항목:
  - Next.js API 라우트 미노출
  - Legacy 정적 페이지 응답
  - Healthcheck 미노출
  - PWA manifest 미노출

## 판단

- 같은 커밋에서 로컬 `npm run verify:mvp`는 통과했다.
- 증빙 삭제, 데이터 보관/삭제 기준, 백업 산출물 변경은 로컬 production 빌드에서 검증됐다.
- 공개 URL이 최신 Next.js 서비스에 연결되지 않는 Railway 외부 설정 문제가 계속 남아 있다.

## 필요한 외부 확인

- Railway Dashboard에서 `honzang-production.up.railway.app` 도메인이 최신 Next.js 서비스에 연결되어 있는지 확인.
- legacy/static 서비스에 붙은 public domain을 최신 서비스로 이전.
- 최신 서비스의 GitHub main branch, Root Directory, Dockerfile build, `DATABASE_URL`, `npm run db:deploy`, `npm run start` 설정 확인.
