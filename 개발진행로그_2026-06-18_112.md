# 개발 진행 로그 112

## 작업 단위
- 분개 상태 변경 마감/중복 가드 커밋(`3b5fcfb3eec956a324cf937d2c223d6e64b8c261`) 이후 공개 Railway 배포 상태를 감사했다.

## 실행 명령
- `RAILWAY_AUDIT_SOFT=1 RAILWAY_EXPECTED_COMMIT=3b5fcfb3eec956a324cf937d2c223d6e64b8c261 npm run audit:railway`

## 감사 결과
- 공개 주소: `https://honzang-production.up.railway.app`
- 기대 커밋: `3b5fcfb3eec956a324cf937d2c223d6e64b8c261`
- `/`: HTTP 200, `text/html; charset=utf-8`, 24489 bytes
- `/api/version`: HTTP 404
- `/api/health`: HTTP 404
- `/manifest.webmanifest`: HTTP 404
- 요약: 5 fail, 0 warn, 0 pass

## 실패 항목
- `Next.js API 라우트 미노출`
- `Legacy 정적 페이지 응답`
- `Healthcheck 미노출`
- `PWA manifest 미노출`
- `보안 헤더 누락`

## 판단
- 최신 커밋은 GitHub `main`에 푸시됐지만 공개 Railway URL은 아직 새 Next.js 앱이 아니다.
- `/api/version`, `/api/health`, `/manifest.webmanifest`가 모두 404라 public domain 연결 또는 서비스 선택 문제가 지속되고 있다.

## 다음 조치
- Railway Dashboard에서 public domain과 Healthcheck Path가 최신 Next.js standalone 서비스에 연결되어 있는지 확인한다.
- 연결 전환 후 `RAILWAY_EXPECTED_COMMIT=$(git rev-parse HEAD) npm run verify:railway`를 실행한다.
