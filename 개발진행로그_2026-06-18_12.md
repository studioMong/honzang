# 개발진행로그 12 - 빈 DB 온보딩 커밋 후 Railway 재점검

## 기준 커밋

- 커밋: `b9ae310daec8155cbef1a6551d8900d01fbe07a6`
- 원격: `origin/main`

## Railway 공개 URL 재점검

- 대상: `https://honzang-production.up.railway.app`
- 명령: `RAILWAY_AUDIT_SOFT=1 RAILWAY_EXPECTED_COMMIT=b9ae310daec8155cbef1a6551d8900d01fbe07a6 npm run audit:railway`

## 결과

- `/`: HTTP 200, legacy/static HTML 응답
- `/api/version`: HTTP 404
- `/api/health`: HTTP 404
- `/manifest.webmanifest`: HTTP 404
- Summary: `4 fail, 0 warn, 0 pass`

## 판단

- 빈 DB 온보딩 개선 커밋은 GitHub `origin/main`에 푸시됐다.
- 공개 Railway 도메인은 여전히 최신 Next.js standalone 서비스가 아니라 이전 정적 서비스 또는 다른 서비스에 연결되어 있다.
- 로컬 `npm run verify:mvp`와 브라우저 확인은 통과했으므로, 남은 배포 완료 조건은 Railway Dashboard의 도메인/서비스 연결 수정이다.

## Railway Dashboard 확인 필요

- `honzang-production.up.railway.app` public domain이 현재 앱 서비스에 연결되어 있는지 확인한다.
- Deploy source commit이 `b9ae310daec8155cbef1a6551d8900d01fbe07a6`인지 확인한다.
- 앱 서비스의 Root Directory가 저장소 루트인지 확인한다.
- 빌더가 루트 `Dockerfile`을 사용하는지 확인한다.
- `DATABASE_URL`이 Postgres 서비스 참조 변수로 연결되어 있는지 확인한다.
- Healthcheck Path가 `/api/health`인지 확인한다.
